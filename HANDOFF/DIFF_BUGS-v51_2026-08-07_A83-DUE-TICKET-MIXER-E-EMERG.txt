DIFF BUGS v50 -> v51 — giro QB-2026-08-07-A83-DUE-TICKET-MIXER-E-EMERG
Sorgente: blob a HEAD 2960f089225b3c80cf56cb839fde871cf9738b3d (git object 2598ae0288aefc29ac3d29c8b2e3b33e4057bb82)
Faccia: BLOB. Misurato prima di comporre (regola A80): CR blob 0 · CR disco 0 · attr text: unspecified;
        i due contenuti spogliati dei CR rendono lo STESSO sha256 => coppia canonica => base = faccia blob.
        BUGS ha UNA FACCIA SOLA (disco == blob al byte): l'anomalia del congedo 07/08 e' confermata, non ereditata.
PROPOSTA — il canonico NON e' stato modificato. Nessun commit, nessun push.
Contesto ampio (-U2) per rendere leggibile la collocazione. Esito: 1039 -> 1065 righe.
VERIFICA: il corpo, riscritto con intestazione git pulita, passa 'git apply --check' (prova nel referto A83).
============================================================

--- a/BUGS_QBEATS.md (v50 @ 2960f08)
+++ b/BUGS_QBEATS.md (v51 PROPOSTA)
@@ -1,6 +1,6 @@
 # BUGS_QBEATS — Tracker centralizzato bug e tech debt
 
-**Versione:** 50
-**Ultima modifica:** 2026-08-04
+**Versione:** 51
+**Ultima modifica:** 2026-08-07
 **Autore iniziale:** CC chat principale 26/05/2026 sera
 **Repo:** `C:\Users\BULLFROG\Desktop\ANTIGRAVITY\Q-BEATS\`
@@ -146,4 +146,29 @@
 - **Dominio:** CC.
 
+### TD-mixer-copre-endshow — il mixer sopravvive a END SHOW, e col disegno Ⓐ copre l'unica uscita (🔴 OPEN ALTA / 🚨 BLOCCANTE PALCO — **PROPOSTA, non assegnata: decide Mauro**)
+- ⚠️ **DUE BUG DISTINTI SOTTO LO STESSO TITOLO — NON chiuderne uno credendo di aver chiuso l'altro.** **(a)** `MixerOverlayView.swift:24 @ 2960f089225b3c80cf56cb839fde871cf9738b3d` misura il pannello sullo SCHERMO invece che sull'area safe: `.frame(height: UIScreen.main.bounds.height * 0.21)`. È l'**unica occorrenza di `UIScreen` in tutta l'app** (sweep su tutti i `.swift` tracciati, due casse; controllo positivo `geo.size` = 25 su 9 file, `GeometryReader` = 10 — lo stesso file usa `geo.size` altrove). Conseguenza aritmetica: il pannello è alto `844 × 0,21 = 177,24` mentre l'alloggio che copre vale `geo.size.height × 0,21 = 160,23` ⇒ **17,01 pt più alto del suo posto**, e in ambienti dove finestra ≠ schermo (iPad in multitasking) misura una grandezza che non c'entra. **(b)** il mixer **sopravvive** al passaggio a `.fineSetlist`. ⛔ **Il fix di (a) NON risolve (b):** anche riportando il pannello a 160,23 pt il suo bordo superiore salirebbe a y 649,77 e la barra del disegno Ⓐ (y 738-794) resterebbe coperta al **100 %**.
+- **Fatto — nessuno azzera `showMixer` al cambio di stato** (verificato a fonte con sweep PER EFFETTO su chi scrive il campo, non per nome della funzione attesa): `showMixer` compare **9** volte in tutto `ios_app/`, e cassa sensibile e insensibile rendono lo stesso numero. Le **due** sole scritture a `false` sono gesti dell'utente — `LiveView.swift:201` (zona `Color.clear`) e `MixerOverlayView.swift:34` (tap sul pannello), entrambe `@ 2960f089225b3c80cf56cb839fde871cf9738b3d`. **Nessun `onChange`, nessun ramo di `playbackState`, nessun reset** lo riporta a `false`. Controllo positivo della forma sulle assegnazioni, stesso corpus: `playbackState = .` rende **19**. ⇒ un mixer aperto durante l'ultima sezione **arriva a END SHOW già aperto**.
+- **Il pannello è OPACO, ed è questo che rende il difetto invisibile:** `MixerOverlayView.swift:25 @ 2960f089225b3c80cf56cb839fde871cf9738b3d` → `.background(Color(hex: "#16161a"))`, tinta unita. Le quattro occorrenze di `opacity` nel file (`:29`, `:54`, `:68`, `:73`) sono tutte su altro — filo di bordo, etichette, tint degli slider, canali disabilitati — **nessuna tocca il fondo**. Anche lo strato che lo contiene (`LiveView.swift:194-207`) non porta `opacity`. Ciò che sta sotto non si vede: per niente.
+- **Geometria, col disegno Ⓐ ratificato — la barra finisce DENTRO il pannello, al 100 %:** riferimento misurato = AREA SAFE, perché `LiveView.swift:76` apre `GeometryReader` **senza** `ignoresSafeArea` (l'unico `ignoresSafeArea` del file è a `:89`, sul solo colore di fondo). Baseline dichiarata iPhone 13 (390×844, inset 47/34 ⇒ `geo.size.height` = 763): pannello y **632,76 … 810** · zona di chiusura alta y **258,89 … 632,76** · barra Ⓐ y **738 … 794** (dal CSS del deliverable CD del 07/08: `.foot` con `padding:0 20px 50px` più `.cta` con `height:56px`, in un frame flex-column da 844). ⇒ la barra sta **interamente** dentro il pannello, 105,24 pt sotto il suo bordo superiore. **A HEAD, senza il disegno Ⓐ, non è così:** i due bottoni sono centrati (`FineSetlistView.swift:22`, VStack senza allineamento verticale) e cadono nella zona di chiusura, non nel pannello. È il disegno a portarli da fuori a dentro.
+- **Sotto quella banda ci sono i quattro `Slider`, e scrivono SU DISCO — catena INTERA, nessun passaggio dedotto:** `MixerOverlayView.swift:62` (`set:` del `Binding` → `audioEngine.setChannelVolume(index + 1, volume:)`) → `AudioEngine.swift:1441-1471`, il cui **unico** guard è `guard (1...4).contains(channel)`: **nessun guard sullo stato del motore**, e la funzione è stata letta per intero → `:1468` `self.appSettings.updateChannelVolumes(...)` → `AppSettings.swift:40-46` → `save()` → `AppSettings.swift:35-38`, `UserDefaults.standard.set(data, forKey: Self.udKey)` con chiave `com.bullfrog.qbeats.appSettings`. Tutti gli indirizzi `@ 2960f089225b3c80cf56cb839fde871cf9738b3d`. **E il valore TORNA all'avvio successivo:** `AudioEngine.swift:109` inizializza il campo con `AppSettings.load()`, e `applySettings` (`:211-218`) riscrive `chNVolume` e `chNMixerNode.outputVolume` dai valori caricati. Rafforzativo misurato: perfino a motore smontato la scrittura passa, perché nel `didSet` (`:109-114`) `save()` è chiamata **prima** di `applySettings`, che è l'unica ad avere un early-return (`:200-201`).
+- **⇒ LA CONSEGUENZA CHE FA LA SEVERITÀ.** A fine show, al buio, il pollice cerca l'unico pulsante dove il disegno lo mette e trova uno `Slider`. I due canali sempre abilitati sono **CLICK** e **BACKT** (`MixerOverlayView.swift:15-16`, `enabled: true`; CH3/CH4 portano `.disabled(!enabled)` fuori da Pro Mode). Un tocco può azzerarne uno **e lasciarlo azzerato allo show successivo**, senza alcun segnale: il concerto dopo parte muto.
+- ✅ **NON È UN VICOLO CIECO, e va detto esplicitamente per non gonfiare il ticket.** La zona di chiusura alta — `LiveView.swift:197-202 @ 2960f089225b3c80cf56cb839fde871cf9738b3d`, `Color.clear` alta `geo.size.height * 0.49` ≈ 374 pt, larghezza piena, con `.contentShape(Rectangle())` e `.onTapGesture` che porta `showMixer` a `false` — resta **scoperta e non ostruita**: sopra di lei non c'è nulla. Un tocco lì chiude il mixer e la barra torna premibile. Il difetto non è «non si esce»: è che **l'unica uscita diventa invisibile** e il gesto sbagliato può fare danno permanente.
+- **Ⓨ (aprire il mixer MENTRE si è a END SHOW) NON è chiusa per costruzione — è chiusa per OCCLUSIONE, che è resa e non struttura.** Le tre aperture non hanno **alcun** gate di stato: `LiveView.swift:116-121` (trascinamento verso il basso sul blocco teleprompter) · `TransportView.swift:95-101` (tap sulla maniglia a capsula 28×3 pt, gattata solo da `if !session.showMixer`) · `TransportView.swift:104-108` (trascinamento verso l'alto su tutta la pulsantiera). Vivono nella VStack principale di `LiveView` (`:96-126`), fuori da ogni ramo condizionale di stato, e sono coperte solo perché `FineSetlistView` è disegnata dopo nello ZStack con fondo opaco — proprietà che cambierebbe da sola se qualcuno riordinasse lo ZStack o rendesse il fondo non opaco. ⇒ **servono i tre gesti disarmati esplicitamente in `.fineSetlist`**, non lasciati alla stratigrafia.
+- **NOTA, non causa** (da guardare quando si mette mano, non da chiudere qui): la scrittura su UserDefaults è **DOPPIA** — `save()` dentro `updateChannelVolumes` (`AppSettings.swift:45`) più `save()` dal `didSet` di `appSettings` (`AudioEngine.swift:111`), che scatta perché chiamare un metodo `mutating` su una proprietà con osservatore lo innesca. Quante volte per gesto dipende da quanti `set` emette il trascinamento dello `Slider`: **NON MISURATO**, è runtime. Ma il doppio percorso è nel codice, non dedotto — è I/O ripetuto durante un gesto continuo.
+- **SI ARMA CON ⟦S5b⟧, NON PRIMA.** Oggi END SHOW è irraggiungibile: `QLiveRootView.swift:110` gatta il player su `if let runner = roomSession.runner`, e lo slot non ha mutatori — `extension QLiveSession` rende **0** in tutto il corpus (controllo positivo `extension ` = 11), `SetlistRunner(` rende **0**, e l'unica occorrenza di `.runner` è una lettura. Il ticket descrive un difetto **reale e misurato oggi** che diventa **raggiungibile** quando ⟦S5b⟧ apre la stanza.
+- **Stato: PROPOSTA di severità 🔴 OPEN ALTA / 🚨 BLOCCANTE PALCO — il valore lo assegna MAURO, non è assegnato qui.** Motivo della proposta: la conseguenza non è una schermata scomoda, è un canale audio azzerato in modo **permanente**, su un percorso che si raggiunge dal flusso normale di fine show. Precedente di forma: `TD-fineshow-bottoni-morti`, la cui severità è stata **decisa da Mauro il 04/08/2026** dopo essere stata proposta. ⚠️ **La collocazione in §1.1 segue la PROPOSTA:** se Mauro assegna un valore diverso, il ticket va spostato.
+- **Dominio:** CC (disarmo dei tre gesti, reset di `showMixer`, correzione di `UIScreen` in `geo`) + CD (se e come il mixer debba esistere a END SHOW: è disegno, non cablaggio).
+
+### TD-emerg-bottone-morto — il tasto «emerg» della pulsantiera è premibile e non fa nulla, OGGI, durante uno show (🔴 OPEN ALTA / ⚠️ NON BLOCCANTE PALCO — **PROPOSTA, non assegnata: decide Mauro**)
+- ⚠️ **È RAGGIUNGIBILE OGGI — è ciò che lo distingue dagli altri bottoni morti di casa.** Non aspetta ⟦S5b⟧, non è dietro alcun gate: sta nella pulsantiera del player, visibile e premibile durante uno show, sul percorso normale. Gli altri due della famiglia (`TD-fineshow-bottoni-morti`, i bottoni di `FineSetlistView`) vivono su una schermata che a HEAD non si raggiunge.
+- **Fatto — verbatim dal blob, `TransportView.swift:90-92 @ 2960f089225b3c80cf56cb839fde871cf9738b3d`:**
+  `RubberBtnView(label: "emerg", glyph: "⚠", danger: true,` · `    disabled: false,` · `    scaleFactor: scaleFactor) { /* navigazione Vista LISTA — Fase successiva */ }`
+  La closure è **vuota**: contiene solo un commento segnaposto. `disabled: false` lo rende **premibile**, e `danger: true` gli dà il trattamento visivo che promette un'azione d'emergenza.
+- **Perimetro misurato:** la pulsantiera è fatta di **sei** `RubberBtnView` (`TransportView.swift:26`, `:30`, `:66`, `:72`, `:76`, `:90`), letti uno per uno con la propria azione. I primi cinque chiamano codice reale (`prevSection`, stop/`waitingForDirector`/`startSetlist`, `nextSection`, `toggleLoop`, `stopBacktrack`). **Il sesto è l'unico muto.**
+- **Regola violata — CD-Q7, «niente bottone morto», citata per indirizzo e non ricopiata:** `HANDOFF/SCALETTA_ATOMI_S6_2026-07-10.md:322 @ 2960f089225b3c80cf56cb839fde871cf9738b3d` (dove la regola è formulata come divieto) · `LIBRO_MASTRO_QBEATS.md:272 @ 2960f089225b3c80cf56cb839fde871cf9738b3d` (la ratifica originale Q7 dell'11/07) · `LIBRO_MASTRO_QBEATS.md:287 @ 2960f089225b3c80cf56cb839fde871cf9738b3d` (applicazione: una CTA anticipata **per non lasciare un bottone morto a schermo**) · `BUGS_QBEATS.md:731 @ 2960f089225b3c80cf56cb839fde871cf9738b3d` (la chiusura precedente della stessa famiglia). Tutti e quattro gli indirizzi sono stati **verificati a fonte** prima di essere incisi qui.
+- **DUE DOMANDE DISTINTE, non una — e la prima non è di CC:** **(1)** *cosa deve fare* un tasto di emergenza sul palco, oppure se debba essere **tolto**: è disegno, e la risposta è di **CD**. Il commento nel codice dice «navigazione Vista LISTA», ma quella è un'intenzione di chi scrisse la riga, **non una ratifica**: nessun canonico è stato interrogato qui su cosa CD voglia, e non va assunto. **(2)** il cablaggio o la rimozione, che è di **CC**, e non può partire prima della (1).
+- **Stato: PROPOSTA di severità 🔴 OPEN ALTA / ⚠️ NON BLOCCANTE PALCO — il valore lo assegna MAURO, non è assegnato qui.** Motivo della proposta: 🔴 ALTA perché è raggiungibile **oggi**, col dito, in performance, e perché un tasto marcato pericolo che non fa nulla è peggio di un tasto assente — chi lo cerca in un momento critico perde secondi. ⚠️ **NON BLOCCANTE** perché lo show va avanti lo stesso: nessuna funzione viva viene persa, e non è una regressione — la riga è così dalla nascita del file. Le due assi sono indipendenti, come dichiara il cappello di §1.1; precedente della stessa combinazione in questa sezione: `TD-injecttestdata-sovrascrive-dati-reali`.
+- **Dominio:** CD (che cosa deve fare, o se va tolto) → poi CC (cablaggio o rimozione).
+
 ## ⚠️ 1.2 — Non bloccanti palco, da chiudere pre-release v1 (🟠 OPEN MEDIA)
 
@@ -1034,4 +1059,5 @@
 | 49 | 2026-08-04 | Mauro + CC + referee | **Nuovo ticket §1.2 `TD-fineshow-bottoni-morti` — i due bottoni di `FineSetlistView` non fanno nulla (doc-only, zero codice).** Il sintomo era già agli atti nei collaudi `59ab33e` (28/06) e 15/07, ma ospitato come causa (iii) di `TD-qlive-libero-limbo`: qui diventa ticket proprio, perché END SHOW è la fine **normale** di ogni show portato a termine, non un flusso pre-produzione. **Causa a fonte:** `ios_app/QBeats/UI/Live/FineSetlistView.swift:19` `Button("BACK TO SHOWS")` e `:21` `Button("RESTART SETLIST")`, entrambe le closure vuote con solo un commento placeholder; blame `899be2ce1da81f0e4a6280904d8d18087a8c1074` (21/05, invariato da allora), verificato a HEAD `c1556e57b1a81fafa7973b8647741ede9c92e6cf`. **TERZO STATO CONTRADDITTORIO NELLO STESSO GLOSSARIO DEL LIBRO**, misurato a HEAD: `:153` Restart Setlist «proposto (CD-3)» mai deciso · `:154` BACK TO SHOWS «attivo» mai cablato · `:155` END SHOW «attivo» per un **bottone che nel codice non esiste con quel nome** — in `FineSetlistView` i bottoni sono due, e nessuno si chiama END SHOW. ⛔ La rettifica della riga del LIBRO **non avviene in questo giro**: il LIBRO non si riscrive, si marca, ed è un atomo a sé. **Severità 🔴 ALTA / 🚨 BLOCCANTE PALCO, DECISA da Mauro il 04/08/2026** (data d'evento, congelata); Stato 🔴 OPEN ALTA / 🚨 BLOCCANTE PALCO. ⚠️ Il ticket resta in §1.2 benché sia bloccante palco — la sezione dichiara di non contenerne: marcato nel campo Stato del ticket, ricollocazione a un atomo doc a sé. ⚠️ **BUCO DI REGISTRO, DICHIARATO QUI PERCHÉ È QUI CHE SI INCIAMPA: v48 NON HA RIGA.** Il registro salta dalla voce **47** a questa. `v48` (`ebbb864c59bbb8b3cff3dbcd5ebbccf756062b3e`, 02/08 — estensione a `r.130`, scadenza della mitigazione da ⟦S6⟧ a ⟦S-EXIT⟧) è committata **senza voce di registro**: prima interruzione in quarantasette versioni. ⛔ **Non si tappa in questo lotto** — completarla dentro il commit di v49 metterebbe in v49 una riga che parla di v48. Atterra in un **atomo doc a sé, dedicato al backfill della voce v48**. |
 | 50 | 2026-08-04 | Mauro + CC + referee | **Marcatura additiva su `TD-qlive-libero-limbo` — le tre cause non sono più tutte vere (doc-only, ZERO parole riscritte).** Misurato a HEAD `c1556e57b1a81fafa7973b8647741ede9c92e6cf`. **(i) MORTA:** `LiveRootView.swift` non esiste più, la forma esatta `setlists.first ??` rende zero e `Setlist.makeDefault` rende zero — tre controlli indipendenti. **(ii) VIVA, ma la premessa è falsa:** il ramo `.collaborativa` di `TransportView.swift:38` esiste tale e quale, ma il default dichiarato è `.standalone` su tre siti (`AppSettings.swift:22`, `AudioEngine.swift:55`, `AudioEngine.swift:280`), coerente con `LIBRO_MASTRO_QBEATS.md:268` — il ramo resta raggiungibile per scelta manuale del ruolo a ogni avvio, non per default automatico. **(iii) VIVA e TRASFERITA:** `FineSetlistView.swift:19`/`:21` restano a closure vuota, ma la materia passa al ticket nuovo `TD-fineshow-bottoni-morti` (v49), non è più questa riga a portarla. ⇒ **La cura registrata a `BUGS_QBEATS.md:312 @ c1556e57b1a81fafa7973b8647741ede9c92e6cf` (scelta d'ingresso A/B) punta a una causa che non esiste più**: risolveva (i), e (i) è morta. Non si cancella: si marca che il suo bersaglio principale è caduto. ⚠️ Il qualificatore «flusso pre-produzione» nel titolo prendeva sostegno anche dalla causa (i): con (i) morta, quel sostegno cade — **titolo e Stato NON toccati**, la riclassificazione, se ce n'è una, è decisione di Mauro. ⚠️ Prova agli atti: `HANDOFF/MISURE_CC_2026-08-03_A17-TRE-CAUSE-LIMBO.txt` — nome con data 03/08, misura eseguita realmente il 02/08 (`createdTime` Drive `2026-08-02T19:14Z`): il nome è un indirizzo, si marca e non si rinomina. |
+| 51 | 2026-08-07 | Mauro + CC + referee | **Due ticket NUOVI in §1.1, doc-only, zero codice.** **(1)** `TD-mixer-copre-endshow` — il mixer sopravvive al passaggio a `.fineSetlist` (nessuna delle 9 occorrenze di `showMixer` lo azzera al cambio di stato; le 2 scritture a `false` sono gesti dell'utente) e, col disegno Ⓐ ratificato il 07/08, il suo pannello **opaco** copre al 100% l'unica uscita — barra y 738-794 dentro pannello y 632,76-810, misurato in area safe. Sotto quella banda stanno i quattro `Slider`, che scrivono su **UserDefaults senza alcun guard sullo stato del motore** e il cui valore **torna all'avvio successivo**: un tocco al buio può azzerare CLICK o BACKT in modo permanente. ⚠️ **DUE BUG DISTINTI nel ticket, da non chiudere insieme:** l'uso di `UIScreen` invece di `geo` (unica occorrenza in tutta l'app, pannello 17,01 pt fuori misura) e la sopravvivenza del mixer — il fix del primo **non** risolve il secondo. ✅ **NON è un vicolo cieco:** la zona di chiusura alta resta scoperta. Si arma con ⟦S5b⟧, non prima. **(2)** `TD-emerg-bottone-morto` — il sesto tasto della pulsantiera (`TransportView.swift:90-92`) è premibile (`disabled: false`), marcato `danger`, e la sua closure è vuota: **raggiungibile OGGI durante uno show**, a differenza degli altri bottoni morti di casa. Viola CD-Q7, citata per indirizzo su quattro siti verificati a fonte. ⛔ **Per ENTRAMBI la severità è PROPOSTA e non assegnata: il valore lo decide Mauro** — precedente `TD-fineshow-bottoni-morti`, deciso da Mauro il 04/08. La collocazione in §1.1 segue la proposta e va rivista se il valore cambia. ⚠️ **Reperto di forma registrato, non riparato:** il registro salta la **voce 48** (la sequenza va 47 → 49). Fonte delle misure: `HANDOFF/MISURE_CC_2026-08-07_A83-DUE-TICKET-MIXER-E-EMERG.txt`, che raccoglie A79/A81/A82. Header bump 50→51. Doc-only. |
 
 ---
