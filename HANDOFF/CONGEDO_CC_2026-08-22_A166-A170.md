# CONGEDO CC — sessione 22/08/2026, mandati A166 → A170

Da: CC · A: **la chat CC che apre dopo di me**, + Mauro + referee

⛔ **Scritto SENZA leggere il congedo del referee**, per mandato. Se i due
divergono, la divergenza e' informazione: non appianatela, misuratela.

⚠️ **Non sono la CC del congedo `CONGEDO_CC_2026-08-21_NOTTE.md`.** Quello firma
`A165-FINE` ed e' di un'altra sessione. **Io ho aperto leggendo quel congedo e ho
lavorato da A166 a A170.**

Marcatura: **[M]** misurato da me alla fonte in questa sessione · **[R]**
riportato da altri, non rimisurato · **[A]** giudizio mio.
⛔ **I tre registri non sono mai mescolati in una stessa frase.**
⛔ **Nessun numero in questo file e' stato scritto senza misurarlo oggi.** Dove
non avevo la misura, c'e' scritto «non misurato».

---

## 1 · STATO — tutto [M], misurato alle 11:08 del 22/08/2026

### HEAD

```
HEAD locale = HEAD remoto = 4629ee9ec943a1ebb8a16a49164aa457a8b99514
```

Letto con `git rev-parse HEAD` e `git ls-remote origin master`.
⛔ **Mai `rev-parse origin/master`**: legge una copia locale.

### I cinque canonici

| canonico | versione | ultimo commit |
|---|---|---|
| `BUGS_QBEATS.md` | **59** | `4629ee9` — **22/08, oggi** |
| `LIBRO_MASTRO_QBEATS.md` | **59** (21/08) | `6527d82` — 21/08 |
| `BOX5_QBEATS.md` | **V29** | `4e57870` — 21/08 |
| `BOX3_QBEATS.md` | **V99** (22/07) — vedi §3.1 | `7c804c1` — **22/07, 31 giorni fa** |
| `HANDOFF/SCALETTA_ATOMI_S6_2026-07-10.md` | **11** (18/08) | `fe2091a` — 19/08 |

### Working tree — «pulito» detto PER INTERO

| misura | valore |
|---|---|
| tracciati modificati o in stage | **0** |
| **NON tracciati (`??`)** | **257** |
| di cui dentro `HANDOFF/` | **252** |
| congedi su disco in `HANDOFF/` | **12** |
| congedi **tracciati** in git | **2** |

⚠️ **«Working tree pulito» e' vero solo sui tracciati.** Con 257 non tracciati
permanenti, la misura R2 «nessun file nuovo comparso» **non si puo' fare per
conteggio**: va fatta confrontando l'elenco. Detta per conteggio e' una
sentinella che non suona.

⚠️ **AUTORIFERIMENTO dichiarato:** questo file, appena esiste, porta i congedi su
disco a **13** e i non tracciati a **11**. E cita `A170`: dopo di me la sonda per
contenuto su `A170` non rendera' piu' zero.

---

## 2 · I MANDATI DI QUESTA SESSIONE — cinque, una riga l'uno

| ID | cosa | prodotto |
|---|---|---|
| **A166** | Sola lettura: origine del numero di battuta, verifica della frase «bit-identici», inventario dello strumento DIAG-A | `HANDOFF/MISURE_CC_2026-08-21_A166-CONTATORE-BATTUTA-ORIGINE.md` — **38 603 byte** |
| **A167** | BUGS v58→v59: caratterizzazione FASE 1 in coda a `TD-direttore-parte-da-bar2` + due ticket nuovi. **Fermato al cancello** | `HANDOFF/DIFF_2026-08-22_A167-…CARATTERIZZAZIONE-BAR2.txt` — ora **marcato SUPERATO** |
| **A168** | Otto correzioni sul testo di A167, fra cui un numero mai misurato. **Fermato al cancello** | `HANDOFF/DIFF_2026-08-22_A168-BUGS-v59-CORREZIONI.txt` — **35 797 byte** |
| **A169** | Le due gravita' ratificate, marcatura del diff superato, commit, push, CI, stampa versionata | commit `4629ee9` + `E:\…\BUGS_QBEATS\BUGS_QBEATS_v59_2026-08-22_4629ee9.md` |
| **A170** | Questo congedo | questo file |

**[M] Il commit di questa sessione — uno solo.**

```
4629ee9ec943a1ebb8a16a49164aa457a8b99514
author = committer = Mauro Martintoni <di_tutto@icloud.com>
2026-08-22 10:58:38 +0200 · zero trailer · 4 file · +1081 / -2
docs(bugs): v59 — caratterizzazione FASE 1 di bar2 su device + due ticket nuovi
```

**[M] CI, detta per NOME e mai «verde» secco:**

| workflow | esito su `4629ee9…9514` |
|---|---|
| **iOS Signed Build** | ✅ **success** — run `32563707531`, `headSha` coincidente, 2m54s |
| **F1 — Build Check** | ⛔ **NON PARTITO** — ultima esecuzione **31/07, failure** |
| **Build LinkHut Diagnostic** | ⛔ **NON PARTITO** — ultima esecuzione **22/05, success** |

⚠️ **[A] «La CI e' verde» sarebbe una frase falsa:** verde e' **un** workflow su
tre configurati.

**[M] La stampa in archivio viene dal BLOB, non dal disco**, e la provenienza e'
provata due volte: `git hash-object --no-filters` della stampa rende
`b30265a2de26703dd5ad6f1459975deeb08057b9`, **identico all'OID del blob**; e
`cmp` della stampa contro il file su disco rende **exit 1** — ed e' proprio
quello a dimostrare che non e' una copia. 349 826 byte, **0 CR**.

---

## 3 · CIO' CHE HO MISURATO E CHE NON E' IN NESSUN CANONICO

⛔ **Tutto quanto segue vive solo nel referto A166 (ora committato) o solo in
chat. Nessuno di questi punti e' inciso in BUGS, LIBRO, BOX3, BOX5 o SCALETTA.**

### 3.1 🚨 BOX3 DICHIARA la propria versione e la propria staleness

**[M]** Il congedo del 21/08 notte scrive, nella pendenza n. 1 e nella tabella
delle versioni: «`BOX3_QBEATS.md` … **senza riga `Versione`**: non dichiara la
propria staleness». **La conclusione e' falsa.** La riga 1 di `BOX3_QBEATS.md` e':

> `BOX3 V99 — 2026-07-22 (AUTOPORTANTE) · HEAD=origin=bfa07eb (⚠️ campo strutturalmente stale by-design, …)`

Dichiara versione, data **e** la propria staleness, con avviso esplicito.
**[M] Lo zero di quella misura era tarato bene** — `^\*\*Versione` rende 0 su
BOX3 e 1 su BOX5 — **ma misurava una FORMA che BOX3 non usa.**

⚠️ **[M] Regge invece l'altra meta':** BOX3 e' fermo a `7c804c1` del 22/07,
**31 giorni** (calcolato oggi, non asserito).

⇒ **[A] La pendenza n. 1 come e' scritta manda qualcuno ad aggiungere una riga
che c'e' gia'.** Non e' corretta in nessun canonico.

### 3.2 🚨 La frase «Garantisce Director/standalone bit-identici» e' imprecisa

**[M]** `AudioEngine.swift:974` @ `6527d82b`. Con zero peer i due modi prendono
lo **stesso ramo** (`:971` e' una disgiunzione) e il corpo `:972-999` e' testo
identico. **La divergenza sta altrove, in due punti misurati:**

**(a) Il bridge non protegge tutte le funzioni Link.** In `LinkEngine.mm` @ HEAD
l'early-return su `enabled_` copre **otto** funzioni ma **non** queste due:

```
111: uint32_t link_engine_num_peers(...)   -- NESSUN guard
123: void link_engine_set_quantum(...)     -- NESSUN guard
```

⇒ a `:991` `link_engine_start_at_beat_zero` e' **guardata**: con Link acceso
esegue un `ABLLinkSetIsPlayingAndRequestBeatAtTime` completo, con Link spento e'
un **no-op**. A `:968` `num_peers` legge `numPeers_` **in entrambi i casi**.

**(b) Dal QUARTO buffer i due modi divergono sul percorso audio.**
`AudioEngine.swift:2267-2290`: `linkSyncSkipBuffers = 3` e' identico, ma poi
`.direttore` va su `link_engine_assert_session_state` (`:2284`) e `.standalone`
su `link_engine_sync_phase` (`:2286`), che **riscrive la posizione di beat**
(`:2287`, `:2290`).

⚠️ **[M] `linkEnabled` e `_linkMode` sono assi INDIPENDENTI** — `linkMode` ha una
sola scrittura in tutto il sorgente (`SettingsView.swift:30`), `linkEnabled` e'
deciso da `setLinkEnabled(_:)` @ `:1295`. **Il ramo d'avvio non consulta
`linkEnabled` in nessun punto.**

⇒ **[A] Nessuno ha corretto ne' marcato quella frase. E' ancora nel codice.**

### 3.3 Il tick 1 arriva su `main` PRIMA che lo stato diventi `.playing`

**[M]** Due `DispatchQueue.main.async` emesse dalla stessa esecuzione su
`audioQueue`, in quest'ordine di programma: `:2375` (il `send(tickN)`, dentro le
`scheduleNextBuffer()` di `:993-995`) e **poi** `:1024` (`isPlaying = true`,
`playbackState = .playing`). `DispatchQueue.main` e' seriale FIFO.
⇒ **l'handler di `LiveView.swift:351` gira per il tick 1 prima del flip di
stato.** ⛔ Non ne traggo conclusioni: e' un ordine di accodamento misurato.

### 3.4 Quattro riferimenti di riga SCADUTI nei commenti del codice

**[M]** Verificati ancora presenti oggi. **Non li ho toccati.**

| commento | cita | a HEAD e' |
|---|---|---|
| `LiveView.swift:354` | `AudioEngine.swift:635` | **`:874`** (635 e' una graffa di chiusura) |
| `AudioEngine.swift:764-765` | `loadSection (936-944)` · `start() (~634)` | **`:1147`/`:1151`** · **`:878`** |
| `AudioEngine.swift:2273` | «guard a riga 1359» | **`:2196`** |
| `LiveView.swift:71` | «guard :63» | **`:67`** |

### 3.5 Altri fatti misurati, non incisi

- **[M]** La stringa «Battuta X di Y» **non esiste nel sorgente**: compare una
  sola volta, in un **commento** (`LiveView.swift:14`). Il testo reso da
  `BarCounterView.swift:23-32` e' **`"Bar " + current + " of " + total`**.
- **[M]** `startCountIn(for:)` (`AudioEngine.swift:1561-1563`) e' **un
  passacarte**: chiama `start()` e nient'altro; il parametro non e' usato.
- **[M]** `session.currentBar` ha **una sola scrittura** in tutto il progetto
  (`LiveView.swift:394`) piu' l'inizializzatore a 1 (`LiveSession.swift:19`), e
  **non viene mai incrementata**: viene ricalcolata a ogni tick.
- **[M]** Il titolo del congedo del 21/08 notte dichiara «mandati **A150 →
  A165**», ma per mtime A151 (15:23), A152 (15:31) e A153 (15:40) precedono il
  **primo** artefatto di quella sessione (A155, 18:53), con un buco di **3h13'**
  in mezzo — e il congedo stesso li attribuisce all'altra sessione.
  ⇒ **[A] per artefatto quella sessione e' A155 → A165.**
- **[M]** Esistono **tre** congedi datati 21/08 (`…-21.md` tracciato, `…_SERA.md`
  tracciato, `…_NOTTE.md` non tracciato). Il NOTTE avvisa **solo** contro il SERA.
- **[M]** Inventario DIAG-A: il ramo `feat/diag-first-beat-and-beat-drop-and-3-4-long`
  esiste sul remoto a `b239b1d6…`, i due commit sono antenati del ramo e **non**
  in master; `DIAG-A` rende **0 su master** e **10 sul ramo**; merge-base
  `cb92faa`, **master avanti di 200 commit**; `armSharedJoin` **non esiste sul
  ramo** (0 hit, controllo positivo 5 su master) ⇒ la regione T3-T8 e' stata
  riscritta da `ee0cbc0`.

---

## 4 · LE TRAPPOLE DI MISURA — otto, con la forma esatta della sonda

### ① [M] `find -iname '*A1NN*'` conta gli HASH di git

Gli ID mandato `A1NN` sono **stringhe esadecimali valide**. Il controllo positivo
su `A164` rendeva **5**; tre erano nomi di blob:

```
.git/objects/0a/a1649626cc6f10d1e551010119944046fb5dd8
.git/objects/cc/57710693b23c683c17a164b734ddd44f8b22ee
.git/objects/ef/5a07fc883f0e01d91a1192d6a164dcf5207108
```

⛔ **Sonda che sbaglia:** `find <repo> -iname '*A164*'`
✅ **Sonda che vede:** `find "$R" -path "$R/.git" -prune -o -iname "*A164*" -print`
⇒ **una sonda per nome non potata puo' rendere una collisione FALSA.**

### ② [M] La sonda ID per CONTENUTO ha rumore dai log di device

Il controllo **negativo** `A176`, che deve rendere zero, rendeva **1 su repo e 7
su E:**: sei `LOG/RUN/…/td17_*.log` contengono `[1A176]` come request-id WiFi.
Stessa cosa per `A166`: `uuid=1A166`, `[A1666]`, `398BA166-…`.
✅ **Cura: leggere hit per hit, non per conteggio**, ed escludere `LOG/`.

### ③ [M] Terza classe, trovata OGGI: payload base64 dentro gli HTML di CD

`A170` rendeva **1 hit** su `E:`, dentro
`DA_CD_PER_CC/11_07_2026/1Q-BEATS/Q-BEATS Vista LIVE v2 (standalone).html`:

```
…p3BY52hM1iTutshxZrrNl+hakOkVaLQe83Snmea8A170CUu9d04erejrQ7n4NHh7…
```

⇒ **tre classi di rumore distinte sulla stessa sonda.**

### ④ [M] `$?` dopo una PIPE legge l'ultimo comando, non quello che ti interessa

⛔ **Sonda che sbaglia:** `gh run watch <id> --exit-status | tail -12; echo "exit = $?"`
— quello `0` era l'exit di **`tail`**. Non diceva niente sulla CI.
✅ **Sonda che vede:** `gh run view <id> --json conclusion`
⚠️ **[A] Stessa famiglia della trappola ereditata `grep -c` dentro un `||`: un
codice d'uscita che arriva dal comando sbagliato.**

### ⑤ [M] Apostrofo curvo vs dritto in un `grep -F`

`grep -cF "…rimosso l’innesco"` rendeva **0**; il file porta l'apostrofo dritto
`'` e la stessa sonda rende **1**. ⇒ **un falso zero da un carattere.**

### ⑥ [M] `cmp` disco-vs-blob = exit 1 su file a due facce NON e' divergenza

`BUGS_QBEATS.md`: disco **351 036 byte / 1210 CR**, blob **349 826 byte / 0 CR**.
Differenza = **1210**, esattamente il conteggio dei CR.
✅ **E' proprio quell'exit 1 a provare che la stampa viene da `git show` e non da
una copia del disco.**
⚠️ `.gitattributes` mette `-text` su `HANDOFF/**`, `DESIGN/**`, BOX3 e BOX5 —
**ma NON su BUGS e LIBRO**, che restano a due facce.

### ⑦ [M] Un controllo positivo NON TARATO non prova niente

Cercando la stringa «Battuta» avevo messo accanto `grep -rn 'Brano'` come
controllo positivo: rende **0**. Non provava che la sonda vedesse.
✅ Sostituito con `grep -rn 'var body' --include='*.swift'` = **53**.

### ⑧ [M] `rev-list --count` con pathspec semplifica la storia

Puo' sotto-contare. Misurato con **tre** forme — semplificata, `--full-history`,
`--follow` — tutte e tre rendono **5**, e il controllo positivo senza pathspec
rende **200**. ⇒ qui non ha morso, ma la forma singola non basta a saperlo.

---

## 5 · COSA LASCIO IN SOSPESO — senza addolcirlo

| # | pendenza | stato |
|---|---|---|
| 1 | **🚨 La pendenza n. 1 del congedo 21/08 e' FALSA nella sua ragione** (§3.1). BOX3 dichiara versione e staleness. **Non corretta in nessun canonico** | aperta, segnalata **tre volte** in questa sessione |
| 2 | **🚨 `BOX3_QBEATS.md` fermo da 31 giorni** — questa meta' regge | aperta |
| 3 | **🚨 La frase «bit-identici» resta nel codice, imprecisa e non marcata** (§3.2) | aperta |
| 4 | **Quattro riferimenti di riga scaduti nei commenti** (§3.4). Non toccati | aperta |
| 5 | **🚨 11 congedi su 13 NON tracciati** in un repo **pubblico**, contando questo | aperta, peggiorata di 1 da me |
| 6 | **`F1` non passa dal 25/04** ([M] misurato in A166), ultime due esecuzioni 31/07 **fallite** | aperta |
| 7 | **`Build LinkHut Diagnostic` non gira dal 22/05** | aperta |
| 8 | **Le TRE righe provvisorie del ticket bar2 sono dichiarate, NON chiuse:** la ④ ha 3 giri sugli 8; Sintomo ② e «rientro senza STOP» **senza conteggio** | ⛔ **provvisorio, non chiuso** |
| 9 | **La riga ⑤ della matrice resta NON ESEGUIBILE** finche' `TD-mode-picker-senza-solo` non e' riparato | bloccata |
| 10 | **L'atomo di strumentazione e' PROPOSTO e NON RATIFICATO.** [M] Il costo e' Layer 3, un solo file (`LiveView.swift:351-395`) | non ratificato |
| 11 | **A169 non ha lasciato alcun referto su disco.** Il suo unico atto scritto e' il messaggio di commit | aperta |
| 12 | **Doppia serie di diff per la stessa v59** in `HANDOFF/`: A167 e A168. A167 porta in testa la marcatura **SUPERATO** ed e' committato cosi'. Non cancellato | aperta per scelta |
| 13 | **`TD-direttore-parte-da-bar2`: causa ⬜ NON ATTRIBUITA, caccia manuale SOSPESA, gravita' INVARIATA** 🔴/🚨 | ⛔ **non chiuso** |

---

## 6 · I MIEI ERRORI — sette, e il primo e' il peggiore

**[M] 1. In A167 ho trascritto nel canonico un numero che non era una misura.**
La cella «5 + 3 controprova» era la **prescrizione** del referee, mai un
conteggio eseguito. Nello stesso mandato ho verificato alla fonte **due**
affermazioni falsificabili — il selettore Mode e il contratto ⟦S-EXIT⟧ — e le ho
confermate entrambe. **Ma un numero non l'ho interrogato**, perche' il mandato
diceva «i fatti non si toccano» e io l'ho letto come «i numeri sono fatti».
⚠️ **[A] Un numero prescritto non e' un fatto. L'ha preso il referee in A168, non
io** — ed e' esattamente la classe di errore che il congedo che avevo in mano
metteva come lascito finale.

**[M] 2. Ho contato 14 congedi invece di 12.** `grep -i 'congedo'` cattura anche
`DIFF_S4K_congedo-tastiera_2026-07-29.txt` e il suo `rev2`, che congedi non sono.
Corretto **prima** di riportarlo.

**[M] 3. Ho stampato un conteggio gonfiato prima di accorgermene.** La prima
sonda per nome su `A164` rendeva **5**: l'avevo gia' messa a schermo come
controllo positivo quando ho visto che tre erano hash di `.git/objects`
(trappola ①). Corretta nello stesso giro.

**[M] 4. Ho usato un controllo positivo non tarato** (`'Brano'` = 0, trappola ⑦).
Scartato e sostituito.

**[M] 5. Ho scritto una sonda di integrita' con l'apostrofo curvo** e ho ottenuto
un falso ⛔ 0 (trappola ⑤). Ripreso nella stessa esecuzione.

**[M] 6. Ho letto l'esito della CI dal comando sbagliato** (trappola ④). L'ho
ripreso io prima di riportarlo, ma **l'avevo gia' scritto** nella riga di comando.

**[M] 7. Ho lanciato uno script Python senza impostare l'encoding di stdout** e
si e' interrotto su console `cp1252`. ✅ **Il file bersaglio e' rimasto intatto** —
verificato sha256 — perche' l'interruzione e' caduta in fase di verifica ancore,
**prima** di qualunque scrittura. ⚠️ **[A] E' andata bene per come era ordinato lo
script, non per merito mio.**

⚠️ **[A] Le mie ipotesi:** non ne ho di rivelatesi false. **Non e' merito:** dove
non avevo la misura mi sono fermato o ho dichiarato la divergenza.

---

## 7 · [A] CIO' CHE LASCIO A CHI APRE

⛔ **Un numero che nessuno ha misurato entra in un canonico travestito da
misura, e da li' non esce piu' da solo.** Il «5 + 3» era una prescrizione. Aveva
la forma di un dato, stava in una cella di tabella accanto a numeri veri, e io
l'ho scritto. **La difesa non e' leggere meglio: e' chiedersi, per ogni numero,
CHI l'ha contato e QUANDO.** Se la risposta non c'e', il numero non e' un dato.

⚠️ **La seconda, misurata tre volte oggi in tre forme diverse: uno zero non e'
un fatto finche' non sai cosa vede la sonda.** Tre classi di falso positivo sulla
**stessa** sonda ID — hash di git, log di device, payload base64 — e un falso
**zero** da un apostrofo. Il controllo positivo nella forma identica e' l'unica
difesa che ha funzionato ogni volta.

⛔ **Terza, per chi eredita questo file: NON crederci sulla parola. RIMISURA.**
Il congedo che ho ereditato reggeva quasi tutto e aveva **una conclusione falsa**
proprio nella sua pendenza n. 1 — e l'ho trovata **solo perche' ho rifatto la
misura invece di rileggerla**. **Questo congedo non e' diverso.** Marcalo **[R]**
e comincia da li'.

---

### Controllo d'integrita' di QUESTO file — sul CONTENUTO, non sulle sezioni

**Prima riga attesa:**
`# CONGEDO CC — sessione 22/08/2026, mandati A166 → A170`

**Stringhe obbligatorie — se una manca, il file e' arrivato mutilato:**
`4629ee9ec943a1ebb8a16a49164aa457a8b99514` · `BOX3 V99 — 2026-07-22 (AUTOPORTANTE)` ·
`link_engine_num_peers` · `5 + 3 controprova` · `b30265a2de26703dd5ad6f1459975deeb08057b9` ·
`NON crederci sulla parola` · e il marcatore di fine qui sotto.

⚠️ **Un controllo che conta le sezioni non vede una sezione vuota.** Contate le
stringhe, non i titoli.

---

*A170-FINE — CONGEDO CC 22/08/2026 COMPLETO*
