diff --git a/BUGS_QBEATS.md b/BUGS_QBEATS.md
index 2b22d23..84df1a1 100644
--- a/BUGS_QBEATS.md
+++ b/BUGS_QBEATS.md
@@ -1,7 +1,7 @@
 # BUGS_QBEATS — Tracker centralizzato bug e tech debt
 
-**Versione:** 41
-**Ultima modifica:** 2026-07-20
+**Versione:** 42
+**Ultima modifica:** 2026-07-22
 **Autore iniziale:** CC chat principale 26/05/2026 sera
 **Repo:** `C:\Users\BULLFROG\Desktop\ANTIGRAVITY\Q-BEATS\`
 
@@ -109,7 +109,8 @@ Documento di riferimento **UNICO** per tutti i bug e tech debt (TD) Q-BEATS. Agg
 - **Verifica avversariale 16/06 (workflow 6 agenti):** tabella 8 roam confermata da re-scan indipendente; **«buco di rete lungo → peer cade» FALSIFICATO** (roam innocuo #3 = buco 7,9 s e peer su; roam fatale #4 = buco 0,5 s); **escluse 7/7** «re-assoc dura / nuovo PRIVATE MAC» e «cross-banda 2.4↔5 GHz» (le fanno tutti i roam → non separano). **Unico separatore pulito = vicinanza al cambio-sezione** (solo #4 entro ≤2 s) **MA a n=1 è inscindibile dall'RX-freeze e può essere coincidenza** (8 roam vs centinaia di cambi-sezione). Confidenza giudice = **media**.
 - **Reperto sorgente (Strategia 1, codice congelato `e608f7d`, sola lettura):** l'app **non ha socket UDP propri** — tutta la sync cross-device è **Ableton Link** (LinkKit, binario chiuso; `LinkEngine.mm`), `numPeers` dal solo booleano `ABLLinkIsConnected`; entitlement multicast presente/necessario alla **discovery**. Il **canale privato dell'app = roadmap futura, non esiste oggi** (conferma Mauro). Quindi «SYNC UDP sopravvissuto» = flussi **unicast** di Link (TX) vivi mentre crolla la **presenza/discovery multicast** (RX-freeze). LinkKit chiuso → l'analisi stringe sul multicast ma non lo chiude.
 - **Mitigazione rete (run dedicata 17-18/06 ~9 h, router dedicato banda-singola 2.4 GHz, roam=0) — è una MITIGAZIONE, NON un fix:** con roam=0 il **peer non è MAI stato perso, catastrofe NON riprodotta** in 9 h (Direttore pulito) → per un concerto **2-3 h** il **router dedicato a banda singola = cura di palco sufficiente** (tesi Mauro). Restano micro-dip RX roam-indipendenti (grappoli ~ogni 15 min, ipotesi GTK-rekey/DTIM) che **NON rompono** la sessione. ⚠️ **NON chiude TD#17:** è un palliativo operativo, il difetto di re-join resta.
-- **Stato:** 🟠 **OPEN MEDIA — ROOT CAUSE CIRCOSCRITTA + MITIGATO (22/06).** Non più bloccante operativo: causa = router-specifico H388X (multicast non transita cross-banda); il VR2800 (router di palco) è pulito (dual-band cross-band stabile 10+ min, device 22/06) + workaround single-band universale. **NON 🟢 CHIUSO:** restano caso roam reale sul VR2800 + run palco 2-3h; fix strutturale = Soluzione C (Fase 6-7). Storia: confermato 11/06, caratterizzato 16/06, Strategia 1 esaurita 20/06. Copertura background/foreground via Bug 4 (`6d1dbbf`) invariata.
+- **CONDIZIONE DI CHIUSURA — ratificata agli atti il 22/07 (decisione Mauro 21/07; LIBRO v40, Sez.2 riga 2026-07-22, commit `131a511`):** TD#17 si chiude con **(A)** una run di palco reale di **2-3 h su VR2800 in banda singola senza perdita peer**, **OPPURE (B) Soluzione C in produzione**. Le due sono **ALTERNATIVE, non cumulative**. Il test cross-banda **sotto roaming NON è condizione di chiusura**: è **caratterizzazione**, in coda, non bloccante (coerente con lo SCOPING 23/06 sopra, che già lo dava «in coda»). Supersede — **sulla sola condizione di chiusura** — la formula del 22/06 di questo ticket («restano caso roam reale sul VR2800 + run palco 2-3h», roam incluso e condizioni in AND); il resto del quadro 22/06 (root cause H388X, VR2800 pulito, single-band raccomandata) resta valido. ⚠️ Fino al 22/07 questa condizione viveva solo in handoff e memorie: per il test R-γ («è in LIBRO_MASTRO o non è ratificato») **non esisteva** — messa agli atti PRIMA in LIBRO (v40), POI qui, nell'ordine giusto.
+- **Stato:** 🟠 **OPEN MEDIA — ROOT CAUSE CIRCOSCRITTA + MITIGATO (22/06).** Non più bloccante operativo: causa = router-specifico H388X (multicast non transita cross-banda); il VR2800 (router di palco) è pulito (dual-band cross-band stabile 10+ min, device 22/06) + workaround single-band universale. **NON 🟢 CHIUSO:** manca la condizione di chiusura ratificata — **(A)** run palco 2-3 h VR2800 banda singola senza perdita peer **OPPURE (B)** Soluzione C in produzione, alternative (bullet **CONDIZIONE DI CHIUSURA** sopra; il roam è caratterizzazione, non condizione). Storia: confermato 11/06, caratterizzato 16/06, Strategia 1 esaurita 20/06. Copertura background/foreground via Bug 4 (`6d1dbbf`) invariata.
 - **[SUPERATO 22/06 — sniffer costruito, root cause circoscritta H388X, VR2800 pulito; vedi AGG sopra.]** NEXT STEP (storico) — Strategia 1 ESAURITA (analisi 20/06, log esistenti, ANALISI-ONLY): verdetto (B) NULL-su-differenziatore.** **n=1 BLINDATO** — unico evento genuino = #4 (`00:59:00`); il secondo `numPeers→0` (#8 @ `05:12:04`) è **stop manuale del test alle 05:12** (Mauro), NON un guasto. **Tutti i separatori falsificati o coincidenza-compatibili:** durata buco RX (#4 ha il PIÙ CORTO, 0,5 s), direzione di banda, RSSI d'atterraggio, atterraggio marginale 5 GHz/«Valid: NO» (lo fa anche il #6 innocuo), settling re-assoc, Data Stall, intervallo inter-roam; l'AUTO-JOIN storm di #4 è **conseguenza** (3/5 trigger DOPO il drop); resta solo la **vicinanza al cambio-sezione (#4 = 1,3 s) ma coincidenza-compatibile** (cambi-sezione ~10% del tempo → ~0,8 roam «vicini» attesi su 8 per caso). **Coppia minima #4 vs #6** (gemelli 2.4→5 marginali, differiscono solo per la vicinanza al cambio-sezione) = esatto A/B da rifare al banco. Cecità dei log DIMOSTRATA: su #4 il drop cade +5,78 s dopo il roam = firma del prune peer a timeout fisso di Link — n=1 sul dato (solo #4), corroborato da sorgente aperta (discovery/PeerGateway.hpp: TTL annunciato 5 s, ALIVE ogni 250 ms, + 1 s di padding nello scheduling del prune "to avoid over-eager timeouts" → prune effettivo ~6 s dall'ultimo ALIVE); il +5,78 s misurato corrisponde ai ~6 s effettivi entro la cadenza ALIVE di 250 ms → valore dal codice, indipendente dalla nostra misura, NON circolare; #8 NON è un secondo dato pulito: il suo drop è lo stop manuale del test; la variabile causale = durata del freeze RX dei beacon multicast, che i log device NON espongono. → **Strategia 2 (banco), obbligatoria.** 🚧 HARDWARE BANCO (verificato sull'unità reale 20/06): (i) transizione 2.4↔5 controllabile = RISOLTO — il pannello dell'H388X provisioned "BULLFROG" (fw AGZHP_1.4.4) espone Band Steering on/off, radio per-banda on/off, canale per banda, potenza TX (dropdown %) → è l'AP di test a costo zero (smentita la riga "TIM non controllabile"); VR2800 non band-steera; OpenWRT = piano B solo per roam "morbido" 802.11v a comando. (ii) cattura multicast = APERTO — il router non fa packet-capture, serve sniffer monitor-mode/mirror/peer-testimone, da risolvere PRIMA del banco. Dettagli: H388X_RIFERIMENTO_ROUTER_TEST.md. **Spec A/B:** roam **2.4→5 marginale entro ≤2 s da un cambio-sezione** vs **lontano (>10 s)**, n≫1; se A≫B la co-occorrenza conta, se A≈B era coincidenza. **Niente fix Layer 2 sui soli log.**
 - **Dominio:** CC (+ rete).
 
@@ -867,6 +868,7 @@ Per data di chiusura, decrescente.
 | 39 | 2026-07-18 | Mauro + CC + referee | **Nuovo ticket §1.1 `TD-qlive-exit-unconfirmed-stop` (doc-only) — uscita-stanza col click attivo = stop non confermato (evento-banda).** 🔴 OPEN ALTA / 🚨 BLOCCANTE PALCO (conseguenza catastrofica innescabile dal percorso di uscita ORDINARIO). Due metà distinte: PRE-ESISTENTE (oggi Q-Live apre dritta nel metronomo, back `LiveHeaderView.swift:30`→`onExit`→`AppRootView.swift:43,61-68`→stop→`link_engine_stop`; UNA sola porta viva oggi, nessuna RoomSwitchBar ancora in Q-Live, verificato a fonte — la chiude il rerouting S6, non l'overlay) e NUOVA (uscite lista/dettaglio dopo S4b/S5, coperta dall'overlay STOP-con-conferma, opzione (iii) CD, freeze atteso). **CANCEL WaitingForDirector ESCLUSO dai band-stop (ESITO A, verificato a fonte):** `.waitingForDirector` raggiungibile solo con `isPlaying==false` (`TransportView.swift:36,58`), click non gira → `stop()` colpisce la guardia `AudioEngine.swift:1638` = no propagazione; resta solo questione nav R7. Causa `:1638→:1645`, propagazione per-peer condizionata a Start/Stop Sync utente (`ABLLink.h:75-82`). Mitigazione operativa fino a S6: non uscire col click attivo, fermarlo prima. Dipendenza: freeze disegno CD (LIBRO v36), da chiudere prima di S6. Bump header 38→39. Doc-only, nessun fix codice. |
 | 40 | 2026-07-19 | Mauro + CC + referee | **Nuovo ticket §1.2 `TD-follower-stop-propaga` + stretta ESITO A di `TD-qlive-exit-unconfirmed-stop` (doc-only) — decisione «Follower gregario» (Mauro 19/07, ratificata referee).** **(1)** Nuovo ticket 🟠 OPEN MEDIA: lo stop di un Follower chiama la STESSA `link_engine_stop` (`AudioEngine.swift:1645`) del Direttore, senza alcun ramo di modalità fra `:1078` e `:1645` (verificato a fonte, `VERIFICA_V-DIR_2026-07-19` sha `99ea6ce1…28ff5f`); `linkEngineHandle` creato incondizionatamente (`:409`); corpo C senza modalità (`LinkEngine.mm:485-496`). Asimmetria: AVVIO differenziato per ruolo (`:991`/`:757`/`:951`), STOP no. Non si vede a 2 device solo perché il Direttore ignora l'inbound (`:533`) [inferenza multi-Follower etichettata, prova 3-device da fare]; latente finché Start/Stop Sync spento. Decisione: `.collaborativa`=subordinata (badge "FOLLOWER" `LiveView.swift:70`), non peer-to-peer simmetrico; un p2p simmetrico sarebbe una 4ª modalità. Fix NON progettato, non in S4b. **(2)** ESITO A: rimosso «impossibile per costruzione»; ponte reale = «nessuno `start()` mentre la view è montata» (non `isPlaying⟹isRunning`, falsificato da `handleAppWakeUp:3053`), con CLAUSOLA-LIMITE promossa da nota (buco `LiveView:406-407` senza guard su `.waitingForDirector` → guard `:1638` passabile in sub-frame); collegato a `TD-follower-stop-propaga` (se risolto, il buco si chiude alla radice). Fonti V-ISP sha `9918c379…0350aa`. Bump header 39→40. Doc-only, nessun fix codice. |
 | 41 | 2026-07-20 | Mauro + CC + referee | **5 ticket NUOVI + regola posizionale ratificata (doc-only).** **Regola nuova in "Convenzioni → Workflow aggiornamento" punto 5:** un ticket nuovo si inserisce IN CODA alla sottosezione di severità che gli compete; la posizione non porta significato, lo porta la severità (ratificata referee 20/07 — prima non era scritta, e i due casi più recenti si comportavano in modo opposto: v39 inserito in coda a §1.1, v40 in testa a §1.2). **§1.1 (🔴):** `TD-injecttestdata-sovrascrive-dati-reali` — `injectTestData` (`QBeatsStore.swift:198-202`, `#if DEBUG`) SOSTITUISCE songs+setlists in RAM, `save()` (`:65-77`) riscrive i 3 cataloghi insieme (`:72`/`:73`/`:74`) ed è innescato da ogni CRUD song (`:83`/`:89`/`:94`/`:99`) ⇒ dati di test su disco al posto dei veri; raggiungibile a 2 tocchi perché la CI archivia in `-configuration Debug` (`ios_build.yml:78`+`:83`) e «⚙ DEBUG» è in Home (`HomeRootView.swift:82`). Recupero provato via backup `.qbeats` [riferito Mauro]. Procedura sicura scritta nel ticket. **§1.2 (🟠) ×3:** `TD-qstage-pill-qlive-morta` — `onSwitch` di Q-Stage cablato al solo os_log (`ShowsListView.swift:83-87`), il buco è nel TIPO (`QStageRootView.swift:11` solo `onExit`) → 3 file non 1 riga; NON regressione S4b (blob OID identici `a61647b`↔`6ded4ab`), morto da `36775a4` 13/07; CD-Q7 disattesa (LIBRO **v30**, non v31); il puntatore «SCALETTA:130» oggi atterra sulla riga «File:» di ⟦S4⟧ che non elenca file Q-Stage = trascrizione mancata. · `TD-app-load-silent-fail` — `.task` di `QBeatsApp.swift:17-25` con `catch` che SOLO logga (`:20-23`); cugino di STRATO diverso rispetto alla famiglia swallow→wipe (quelli coprono lo Store, questo il chiamante). · `TD-qlive-search-keyboard-trap` — tastiera search non chiudibile [device 20/07, Mauro], assenza provata (`QLiveShowsView.swift:138`; FocusState/submitLabel/toolbar/scrollDismissesKeyboard/onSubmit = 0); buco nel freeze CD (nessuna spec di tastiera in `2026-07-11_Q7-Q16.html @ 9994bc0`), domanda CD Q20; **trigger: sale a 🔴 ALTA con ⟦S4L⟧** perché uscire dalla stanza — unico modo noto di chiudere la tastiera — ucciderà la sessione live (LIBRO v37 Sez.2). **§1.3 (🟡):** `TD-setlist-id-orfani` — `deleteSong` (`:92-95`) lascia UUID orfani nei `songIDs`; il sistema li tollera per design (`resolve()` `:152` → `missingIDs`, empty-state Ⓖ `QLiveEmptyStates.swift:228`) ma la politica non è mai stata ratificata: decisione attesa a §8. Bump header 40→41. Doc-only, nessun fix codice. |
+| 42 | 2026-07-22 | Mauro + CC + referee | **TD#17 — condizione di chiusura AGLI ATTI (doc-only), ordine R-γ rispettato: prima LIBRO, poi BUGS.** Nuovo bullet «CONDIZIONE DI CHIUSURA» in §1.1/TD #17 + clausola «NON 🟢 CHIUSO» dello Stato allineata: TD#17 si chiude con **(A)** run di palco reale 2-3 h su VR2800 in **banda singola** senza perdita peer **OPPURE (B)** Soluzione C in produzione — **alternative, non cumulative**; il cross-banda sotto roaming = **caratterizzazione**, in coda, NON condizione. Supersede nel ticket la formula del 22/06 («restano caso roam reale sul VR2800 + run palco 2-3h», AND); il resto del quadro 22/06 invariato (root cause H388X, VR2800 pulito, single-band). Decisione Mauro 21/07; ratifica a monte in **LIBRO v40** Sez.2 riga 2026-07-22 (commit `131a511`) — fino ad allora la condizione viveva solo in handoff/memorie e per R-γ non esisteva. Stato TD#17 invariato: 🟠 OPEN MEDIA, NON bloccante. Bump header 41→42. Doc-only, nessun fix codice. |
 
 ---
 
