diff --git a/BUGS_QBEATS.md b/BUGS_QBEATS.md
index 9e06aa5..4a5094f 100644
--- a/BUGS_QBEATS.md
+++ b/BUGS_QBEATS.md
@@ -169,6 +169,7 @@ Documento di riferimento **UNICO** per tutti i bug e tech debt (TD) Q-BEATS. Agg
 - **DUE DOMANDE DISTINTE, non una — e la prima non è di CC:** **(1)** *cosa deve fare* un tasto di emergenza sul palco, oppure se debba essere **tolto**: è disegno, e la risposta è di **CD**. Il commento nel codice dice «navigazione Vista LISTA», ma quella è un'intenzione di chi scrisse la riga, **non una ratifica**: nessun canonico è stato interrogato qui su cosa CD voglia, e non va assunto. **(2)** il cablaggio o la rimozione, che è di **CC**, e non può partire prima della (1).
 - **Stato: PROPOSTA di severità 🔴 OPEN ALTA / ⚠️ NON BLOCCANTE PALCO — il valore lo assegna MAURO, non è assegnato qui.** Motivo della proposta: 🔴 ALTA perché è raggiungibile **oggi**, col dito, in performance, e perché un tasto marcato pericolo che non fa nulla è peggio di un tasto assente — chi lo cerca in un momento critico perde secondi. ⚠️ **NON BLOCCANTE** perché lo show va avanti lo stesso: nessuna funzione viva viene persa, e non è una regressione — la riga è così dalla nascita del file. Le due assi sono indipendenti, come dichiara il cappello di §1.1; precedente della stessa combinazione in questa sezione: `TD-injecttestdata-sovrascrive-dati-reali`.
 - ⛔ **MARCATURA 18/08 — LA VOCE «Regola violata» DI QUESTO TICKET NON È PIÙ VERA, E LO STATO CAMBIA. Zero parole riscritte sopra: si marca.** **(1)** La «Regola violata — CD-Q7» del bullet `:168` **cade per questo ticket**: CD-Q7 fu decisa per il solo «+» di Q-Stage e la sua lettura generalizzata è **esplicitamente eccettuata** per i pulsanti in coda di lavoro di Q-Live — `LIBRO_MASTRO_QBEATS.md`, riga datata `2026-08-18` sui pulsanti inerti. **(2)** ⛔ **Il ticket NON è «da rimuovere».** Lo Stato del bullet `:170` si legge da qui in avanti: **«in attesa di destinazione, PER SCELTA»** — dichiarazione di prodotto di Mauro del 18/08: «emerg» e METRONOME restano visibili e inerti come **coda di lavoro**, e **nessun attore ne proponga la rimozione come igiene**. **(3)** La domanda *«cosa deve fare emerg»* **ha risposta di prodotto**: collega la modalità dinamica di Q-Live a una **versione a LISTA**, nome «emerg» **provvisorio**, e si cabla **insieme** al `.viewtoggle` del dettaglio (`QLiveShowDetailView.swift:50 @ 321293e18094d9d4f1c167bfc921be1ad216e3ac`) perché sono **la stessa materia**. Resta aperto il **disegno**, dominio CD. **(4)** ⚠️ **FUORI DA QUESTA MARCATURA, e non toccato qui:** il bullet `:163` («È RAGGIUNGIBILE OGGI … sul percorso normale») è stato **misurato falso** il 18/08 — la pulsantiera vive solo dentro `LiveView`, che a HEAD non è raggiungibile da alcun gesto. È materia di un giro proprio, non di questo.
+- ⚠️ **MARCATURA 18/08 — «IL SESTO È L'UNICO MUTO» È FALSIFICATO A FONTE. Il bullet «Perimetro misurato» (`:167`) resta come scritto: si marca, non si riscrive.** ⚠️ Questa marcatura è posata **in coda al ticket e non subito sotto il suo bersaglio**, di proposito: inserirla a `:168` avrebbe spostato di una riga i bullet `:168` e `:170`, che la marcatura precedente cita per numero — si sarebbero spezzati due puntatori interni per guadagnare due righe di adiacenza. Misurato a HEAD `44fea3e378414c300ffd50fcac527c683740735b`: **tre dei cinque** che il bullet dichiara «chiamano codice reale» chiamano funzioni **VUOTE** — `ios_app/QBeats/AudioEngine.swift:1267` `func prevSection() {}` · `:1268` `func nextSection() {}` · `:1269` `func toggleLoop() {}`. **Controllo positivo, forma identica sullo stesso file:** `stopBacktrack` (uno dei cinque) **ha corpo vero** — `:1522-1526`, `audioQueue.async` + `backtrackPlayerNode.stop()`. ⇒ **I tasti muti della pulsantiera sono QUATTRO su sei, non uno.** ⚠️ Resta vera una distinzione di **forma** — `emerg` ha la closure vuota *sul posto* (`TransportView.swift:92`), gli altri tre chiamano una funzione *nominata e vuota* — ma **per il dito l'effetto è identico**. ⛔ Il perimetro del ticket **non si allarga qui**: si registra che la sua premessa comparativa è caduta, e la collocazione delle altre tre porte è decisione da prendere. Misura: `HANDOFF/MISURE_CC_2026-08-18_A117-PORTE.md`.
 - **Dominio:** CD (che cosa deve fare, o se va tolto) → poi CC (cablaggio o rimozione).
 
 ## ⚠️ 1.2 — Non bloccanti palco, da chiudere pre-release v1 (🟠 OPEN MEDIA)
@@ -286,6 +287,7 @@ Documento di riferimento **UNICO** per tutti i bug e tech debt (TD) Q-BEATS. Agg
 - **Lavoro = handoff L2→L3**, rispettando «AudioEngine ignora il setlist» (per design): pubblicare l'azione su un canale engine→L3 (pattern `beatTickSubject`/`@Published`) e far eseguire a L3 ciò che fa il TAP. **Mai** reference diretta engine→`SetlistRunner`. Completa l'handoff a L3 che il codice stesso documenta — non lo bypassa.
 - **Audit di build (§4) — il rischio vero = timing della transizione, non il plumbing:** ogni comando deve passare per la stessa transizione del suo equivalente UI. `nextSection`/`prevSection`/`nextSong` a brano in corso → ri-armatura quantizzata al beat tick (finestra SEAMLESS, `LiveView.swift:31-35`/`263-266`), zona race confine-sezione (cfr. §1.3 «Doppio-click Direttore al confine di sezione» / `BUGS:116-124`) — il percorso L3 **non deve saltarla**. `startSong` esente (audio fermo, nessuna finestra seamless).
 - **Confermato alla fonte (26/06):** `nextSection`/`prevSection` = equivalente UI mid-play **ESISTE** (`TransportView.swift:28`/`:68` → `prevSection()`/`nextSection()`) ⇒ MIDI = **mirror del TAP**. **`nextSong` = NESSUN controllo UI per saltare canzone a brano in corso** (oggi l'avanzamento canzone è solo: fine sezione → standby [audio fermo] → tap → `startCurrentSong`) ⇒ **`nextSong` mid-play via MIDI = comportamento NUOVO**, non "specchia il TAP" → richiede **design di transizione proprio** (la canzone in corso: taglio netto? finisce la sezione? va a standby?) — da decidere Mauro + CD quando si costruisce. `startSong` = mirror dello standby tap (`LiveView.swift:131`), esente.
+- ⛔ **MARCATURA 18/08 — URGENTE: «L'EQUIVALENTE UI MID-PLAY ESISTE» È FALSIFICATO, E LASCIATO COSÌ AUTORIZZA A CABLARE IL MIDI SUL VUOTO. Il bullet sopra resta come scritto: si marca, non si riscrive.** Misurato a HEAD `44fea3e378414c300ffd50fcac527c683740735b`: `TransportView.swift:28` e `:68` **esistono** e chiamano `prevSection()`/`nextSection()`, ma quelle funzioni sono **vuote** — `ios_app/QBeats/AudioEngine.swift:1267-1268`. ⇒ **Il TAP esiste, l'effetto no.** La conclusione «MIDI = mirror del TAP» resta vera alla lettera e **per questo è pericolosa**: specchiare il tap oggi significa specchiare un no-op. ⚠️ **Conseguenza operativa, non un'opinione:** il lavoro di questo ticket **non può chiudersi cablando il MIDI**; prima serve che l'equivalente UI faccia qualcosa. **Controllo positivo, forma identica:** `stopBacktrack` — anch'esso comando MIDI — ha corpo vero (`:1522-1526`), quindi per **quel** comando lo specchio è sano. Misura: `HANDOFF/MISURE_CC_2026-08-18_A117-PORTE.md`.
 - **Priorità:** **dietro TD#17** (affidabilità prima della feature mancante), candidato forte alla prima pausa di Q-Stage. Posizione esatta vs TD#17 = chiamata di Mauro.
 - **Validazione:** device, con tastiera/pedaliera MIDI (Mauro). Parte **Q-Studio** = arriva col container (non ancora costruito) → cablare a un controllo di riproduzione **condiviso** (Live + Q-Studio), non setlist-only.
 - **Dominio:** CC. **Stato:** 🟠 OPEN MEDIA (feature-completion), schedulato.
@@ -396,6 +398,16 @@ Documento di riferimento **UNICO** per tutti i bug e tech debt (TD) Q-BEATS. Agg
 - ⚠️ **MARCATURA — `LIBRO:467` È UN PUNTATORE SCADUTO: il suo bersaglio si indirizza per CONTENUTO. Misurato sul blob a HEAD `ddd5716ab0cc696f21cf0c1ec899ee9ed07adab8`. La riga resta com'è.** Le **due** occorrenze di `LIBRO:467` in questo bullet vanno lette come **«la voce 43 del registro Sez.6 del LIBRO — quella che nomina la proprietà del runner»**. Il numero è deriva pura: la voce 43 stava a `:467` al commit `3164564` del 2026-07-30, a `:475` a `8822598`, e a HEAD sta a **`:476`** (controprova: `sed -n '476p'` rende **1** sulla stringa «proprietà del runner»). Chi deve incidere un numero scriva **`LIBRO_MASTRO_QBEATS.md:476 @ ddd5716ab0cc696f21cf0c1ec899ee9ed07adab8`**, mai il numero nudo. ⛔ **Questa marcatura NON tocca `r.1006`**, che porta la terza occorrenza di `LIBRO:467`: è **registro** = storia. ⚠️ **La qualifica di «PUNTATORE PURO» data a `LIBRO:467` in questo stesso bullet RESTA VERA e non è in discussione**: si corregge la forma dell'indirizzo, non la natura del sito.
 - **Dominio:** CD (decisione) + CC (cablaggio).
 
+### TD-countin-ratificato-mai-costruito — il count-in è ratificato con TRE punti d'attivazione e nessuno dei tre suona (🟠 OPEN MEDIA — **PROPOSTA, non assegnata: decide Mauro**)
+- **La capacità È RATIFICATA, e non da oggi.** `LIBRO_MASTRO_QBEATS.md:166` la incide con **tre** punti d'attivazione: «`.countIn` | Count-in pre-canzone (**4 click**). Attivato in: (1) inizio prima song setlist, (2) tra canzoni, (3) Resume dopo STOP a metà song», ratificato **21/05 Q3=A**. `LIBRO_MASTRO_QBEATS.md:225` ne registra il motivo, che è **di palco** e non estetico: «ri-sincronizzazione mentale del batterista con la band, necessaria per partenza uniforme».
+- **NESSUNO DEI TRE È COSTRUITO — meccanismo intero, misurato a HEAD `44fea3e378414c300ffd50fcac527c683740735b`:** **(1)** l'inizio setlist non esiste affatto (è ciò che costruisce ⟦S5b⟧); **(2)** fra le canzoni il rinvio è **dichiarato nel codice** — `ios_app/QBeats/SetlistRunner.swift:232`, «L1.b v1: niente count-in tra canzoni. Da aggiungere in iterazione successiva.», seguito da `audioEngine.start()`; **(3)** il Resume dopo stop passa da `resumeFromCurrentSection` (`AudioEngine.swift:1247-1254`), che posa lo stato e poi chiama `startCountIn(for:)` — **uno stub**: `AudioEngine.swift:1560-1563`, «`// L3 stub — sostituito da Layer 3 quando disponibile.`», corpo `start()`, che **ignora il parametro e fa partire l'audio subito**.
+- **LA CATENA SI ROMPE DUE VOLTE, e sono interruzioni INDIPENDENTI.** ① Il campo `Song.countIn` (`ios_app/QBeats/Models/Song.swift:21`, «0=nessuno, 1=1 battuta, 2=2 battute») si imposta (`ios_app/QBeats/UI/QStage/SongEditorView.swift:34`, un `Picker`) e si conserva nei backup (`QBeatsBackupManager.swift:217`), ma **nessuno lo legge nei percorsi d'avvio**: sonda sul CAMPO (preceduto da un ricevitore, `(song|currentSong)\??\.countIn`) su `SetlistRunner.swift` + `UI/Live/` → **ZERO**; **controllo positivo, forma IDENTICA, stesso ricevitore, altro campo**: `sections` → **1**, `name` → **1**. ② Anche passando il valore, `startCountIn` lo scarterebbe. ⇒ **Riparare il primo anello non basta.**
+- ⚠️ **TRE OGGETTI DIVERSI SI CHIAMANO `countIn`, e confonderli è il modo tipico di sbagliare questa misura** (successo, ed è documentato in `HANDOFF/MISURE_CC_2026-08-18_A115-COUNTIN.md`): il **campo** `Song.countIn` · lo **stato di motore** `AudioEngine.PlaybackState.countIn` (`AudioEngine.swift:26`) · lo **stato di UI** `LivePlaybackState.countIn(countdown:)` (`Models/LivePlaybackState.swift:5`). Cercare `countIn` nei percorsi d'avvio rende **4 file** e sembra che il campo sia letto: sono **tutte occorrenze dello stato**.
+- ✅ **E il numero «4 click» non è arbitrario: è il valore RATIFICATO.** `ios_app/QBeats/UI/Live/LiveView.swift:271` imposta `session.playbackState = .countIn(countdown: 4)` — cioè **mostra** il conteggio ratificato da `LIBRO:166` **senza suonarlo**. Non è una costante inventata: è il numero giusto senza il suono.
+- **Non è un difetto introdotto da ⟦S5b⟧**, ed è già rotto oggi sul percorso inter-canzone. ⟦S5b⟧ lo rende soltanto **incontrabile**, come per END SHOW. ⇒ Al collaudo di ⟦S5b⟧ «al tocco parte subito, senza conto» **NON va segnalato come guasto**: è questo ticket.
+- **Stato: PROPOSTA di severità 🟠 OPEN MEDIA — il valore lo assegna MAURO, non è assegnato qui.** Precedente: `TD-mixer-copre-endshow` e `TD-emerg-bottone-morto`, severità proposte il 07/08 e decise da Mauro. Motivo della proposta: non c'è perdita di dati né silenzio sul palco. ⚠️ **Ma il motivo ratificato è di palco** (`LIBRO:225`), e questo può valere più di quanto pesi CC. La collocazione in §1.2 segue la proposta e **va rivista se il valore cambia**.
+- **Dominio:** CC. Misure: `HANDOFF/MISURE_CC_2026-08-18_A115-COUNTIN.md` e `HANDOFF/MISURE_CC_2026-08-18_A118-SCHEDA-STRETTA.md`.
+
 ## 📦 1.3 — Backlog (🟡 OPEN BASSA)
 
 ### Ripresa da interruzione (telefonata) riparte da capo — single-device (🟡 OPEN BASSA / da tracciare)
@@ -666,6 +678,15 @@ Documento di riferimento **UNICO** per tutti i bug e tech debt (TD) Q-BEATS. Agg
 - **Impatto:** zero sul comportamento — è un commento. Il rischio è di lettura: chi lo prende per buono conclude che il ramo `.metronome` non è raggiungibile **per assenza di chiamanti**, che a HEAD è falso (la ragione vera è che l'unica chiamata passa `.shows`).
 - **⛔ NON si tocca in un giro doc-only:** è **codice**. Si traccia qui e si corregge nel primo atomo che apre quel file. **Dominio:** CC.
 - **Stato:** 🟡 OPEN BASSA / 📦 BACKLOG.
+### TD-canonici-puntatori-path-stale — `BOX5:324` cita un percorso di file che a HEAD non esiste (🟡 OPEN BASSA — **PROPOSTA, non assegnata: decide Mauro**)
+- **Il fatto, misurato a HEAD `44fea3e378414c300ffd50fcac527c683740735b`:** `BOX5_QBEATS.md:324` cita `ios_app/QBeats/Audio/AudioEngine.swift:370`. Quel percorso rende **0 file**; il percorso reale è `ios_app/QBeats/AudioEngine.swift` — **1 file**, **senza** il segmento `Audio/`. ✅ **Riga e contenuto sono GIUSTI:** la riga `:370` di quel file è `    private var backtrackArmed: Bool = false`, esattamente ciò che la riga di BOX5 afferma. **È sbagliato il solo percorso.**
+- **Perché non è cosmetico:** quella riga di BOX5 sostiene un'affermazione che qualcuno andrà a verificare — che a HEAD **non esiste** uno stato di «sessione armata» e che l'unica occorrenza di `armed` è `backtrackArmed`. Chi apre il percorso citato **non trova il file** e non può concludere nulla: la verifica si interrompe sul puntatore invece che sul fatto.
+- ⛔ **PERCHÉ UN TICKET PROPRIO E NON UN ALLARGAMENTO DI `TD-doccomment-navigate-zero-chiamanti` — argomento tecnico, non di comodo.** Quel ticket dichiara la propria condizione di chiusura: «⛔ NON si tocca in un giro doc-only: è **codice**. Si traccia qui e si corregge nel primo atomo che apre quel file». Un puntatore stale **dentro un canonico** ha la condizione **opposta**: si chiude **in un giro doc-only** e non ha alcun atomo di codice che lo apra. ⇒ Fonderli darebbe a un ticket solo **due condizioni di chiusura che si escludono**, che è il difetto «un nome per due contratti» già pagato con ⟦S4L⟧ e inciso in `LIBRO_MASTRO_QBEATS.md:334`.
+- **Censimento della classe, con controllo positivo:** in questo tracker esistono **tre** ticket col lemma «stale» nel titolo e **nessuno è di questa classe** — `TD-link-indicator-stale` (indicatore **runtime**), `TD-linkkit-commenti-versione-stale` (**commenti nel codice**), `TD-doccomment-navigate-zero-chiamanti` (**commenti nel codice**). Ricerca preventiva: `Audio/AudioEngine` → **0** in questo file, `BOX5:324` → **0**. ⇒ **La classe «puntatore stale dentro un CANONICO» non era tracciata.**
+- ⇄ **RIMANDO INCROCIATO con `TD-doccomment-navigate-zero-chiamanti`:** i due si citano e **non si fondono**. ⚠️ E quel ticket porta lo stesso difetto **su di sé**: cita `QLiveRootView.swift:85` e `:145`, ma a HEAD il commento bersaglio sta a **`:87`** e `:145` è diventata una riga di documentazione su `environmentObject` — deriva prodotta da ⟦S5a⟧. Registrato qui perché non si perda; **la sua riparazione appartiene a quel ticket**, non a questo.
+- **Stato: PROPOSTA di severità 🟡 OPEN BASSA — il valore lo assegna MAURO.** Motivo: zero effetto sul comportamento del prodotto. ⚠️ Contro-argomento, dichiarato invece che taciuto: è in un **canonico**, e i canonici sono la superficie su cui si verifica tutto il resto — un puntatore rotto lì costa più di uno in un commento.
+- **Dominio:** CC. ⛔ **Il fix NON è in questo giro:** `BOX5_QBEATS.md` è fuori dal perimetro di A121. Misura: `HANDOFF/MISURE_CC_2026-08-18_A118-SCHEDA-STRETTA.md`.
+
 ## 1.4 — Backlog UX puro (📦, dominio CD)
 
 Riferimento `LIBRO_MASTRO_QBEATS.md` Sezione 3 deliverable per il dettaglio:
