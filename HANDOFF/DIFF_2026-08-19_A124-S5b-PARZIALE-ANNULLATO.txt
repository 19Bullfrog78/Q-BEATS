ANNULLATO — lavoro parziale del mandato A124, che il referee ha revocato il 19/08/2026 perche' basato su una lettura superata della scheda S5b. SOSTITUITO DA A125. Conservato come storia, NON come autorita': non applicare, non citare come stato del codice.
diff --git a/ios_app/QBeats/SetlistRunner.swift b/ios_app/QBeats/SetlistRunner.swift
index 1e60d7d..ae52989 100644
--- a/ios_app/QBeats/SetlistRunner.swift
+++ b/ios_app/QBeats/SetlistRunner.swift
@@ -56,6 +56,29 @@ final class SetlistRunner: ObservableObject {
     /// transizione SEAMLESS (TD #41 lifecycle).
     @Published private(set) var pendingDisplayUpdate: Bool = false
 
+    // MARK: - Guardia sul ri-armamento (⟦S5b⟧ Cond (c))
+
+    /// True dal PRIMO avvio reale di audio in questo runner — lo alzano i due
+    /// soli entry point che chiamano `prepareAndStartCurrentSection`:
+    /// `startSetlist` e `startCurrentSong`.
+    ///
+    /// ⚠️ SERVE PERCHÉ `primeDisplay` GIRA A OGNI `onAppear`, non solo al primo
+    /// (unico chiamante: `LiveView.swift:231`, dentro l'`onAppear` del player).
+    /// Senza questa guardia una ricomparsa della vista rimetterebbe in STANDBY
+    /// uno show GIÀ IN ESECUZIONE: overlay col nome sopra il player oscurato,
+    /// mentre il click continua a suonare.
+    ///
+    /// ⛔ VIVE SUL RUNNER, NON SULLA SESSIONE, e non è un dettaglio: `LiveSession`
+    /// è uno `@StateObject` PRIVATO di `LiveView` (`LiveView.swift:11`) e si
+    /// azzera con la vista; il runner sopravvive alla navigazione interna della
+    /// stanza. È il solo dei due che sa ancora, al ritorno, se lo show era partito.
+    ///
+    /// Non distingue «in play» da «fermato dopo essere partito», e non deve: la
+    /// domanda a cui risponde è «questo runner è mai partito?». Uno show riavviato
+    /// costruisce un runner NUOVO (`QLiveSession.install(_:)`), quindi riparte
+    /// da `false`.
+    private(set) var hasStartedPlayback: Bool = false
+
     // MARK: - Init
 
     init(setlist: Setlist, store: QBeatsStore) {
@@ -113,6 +136,8 @@ final class SetlistRunner: ObservableObject {
         pendingDisplayUpdate = false
         currentSongIdx = 0
         currentSectionIdx = 0
+        // ⟦S5b⟧ Cond (c) — da qui in poi `primeDisplay` non riarma più.
+        hasStartedPlayback = true
         os_log("[Q-BEATS][L1.b] startSetlist — reset a songIdx:0 sectionIdx:0",
                log: .default, type: .default)
         prepareAndStartCurrentSection(audioEngine: audioEngine, session: session)
@@ -125,6 +150,9 @@ final class SetlistRunner: ObservableObject {
         // Reset esplicito coerente con startSetlist (TD #41 lifecycle).
         pendingDisplayUpdate = false
         currentSectionIdx = 0
+        // ⟦S5b⟧ Cond (c) — è QUESTO il percorso del secondo tap d'ingresso
+        // (`LiveView.swift:134-137`): il primo click reale passa di qui.
+        hasStartedPlayback = true
         os_log("[Q-BEATS][L1.b] startCurrentSong — songIdx:%d sectionIdx:0",
                log: .default, type: .default, currentSongIdx)
         prepareAndStartCurrentSection(audioEngine: audioEngine, session: session)
@@ -256,6 +284,14 @@ final class SetlistRunner: ObservableObject {
     /// per evitare che la videata appaia "vuota" (nome canzone/sezione/next
     /// blank) al primo ingresso prima del tap Play.
     ///
+    /// ⚠️ MARCATURA ⟦S5b⟧ — LE TRE RIGHE QUI SOTTO SONO SCADUTE, si marcano e non
+    /// si riscrivono. Da ⟦S5b⟧ l'ingresso NON è più `.stopped`: è ARMA + STANDBY
+    /// sulla prima canzone, come prescrive `BOX5_QBEATS.md:331` (QL-SHOWS-07).
+    /// L'obiezione storica non si applica: TD #28 tolse lo standby d'ingresso
+    /// (`LiveSession.swift:30-34`) perché mostrava un EM-DASH — cioè uno standby
+    /// SENZA NOME VERO, con nessuna setlist caricata. Con un runner armato il
+    /// nome c'è, ed è quello della canzone che partirà al tocco.
+    ///
     /// Coerente con TD #28 (17/05/2026): stato iniziale Vista LIVE è `.stopped`,
     /// nessun overlay standby — la videata deve mostrare i dati della setlist
     /// caricata già in stato `.stopped`.
@@ -273,6 +309,23 @@ final class SetlistRunner: ObservableObject {
         updateSessionDisplay(session: session)
         session.currentBPM         = section.bpm
         session.totalBarsInSection = Int(section.repetitions)
+        // ⟦S5b⟧ Cond (b) — ARMAMENTO D'INGRESSO. Il player si monta FERMO e ARMATO:
+        // titolo della prima canzone che pulsa sopra il player oscurato
+        // (`LiveView.swift:129` opacità 0,10 · `:132-138` overlay + tap-ovunque ·
+        // `StandbyOverlayView.swift:18-24` e `:31-35`). Il click NON parte: parte al
+        // tocco successivo, che va a `startCurrentSong` e suona QUESTA canzone
+        // (`currentSongIdx` è ancora 0).
+        //
+        // Il nome giusto è `currentSong`, NON `nextSong`: `.standby(nextSongName:)`
+        // significa «la canzone che partirà al prossimo tap» — nel ramo standby fra
+        // due canzoni quel nome è letto DOPO `currentSongIdx += 1` (:343-345), quindi
+        // è sempre la corrente-dopo-l'avanzamento. All'ingresso, con indice 0, è la
+        // PRIMA. Usare `nextSong` qui darebbe la SECONDA canzone.
+        //
+        // ⛔ Cond (c): non riarma uno show già partito. Vedi `hasStartedPlayback`.
+        if !hasStartedPlayback, let song = currentSong {
+            session.playbackState = .standby(nextSongName: song.name)
+        }
     }
 
     // MARK: - Closure end-of-section (autopropagante)
diff --git a/ios_app/QBeats/UI/QLive/QLiveRootView.swift b/ios_app/QBeats/UI/QLive/QLiveRootView.swift
index 6f5561f..d6fd2a9 100644
--- a/ios_app/QBeats/UI/QLive/QLiveRootView.swift
+++ b/ios_app/QBeats/UI/QLive/QLiveRootView.swift
@@ -101,7 +101,23 @@ struct QLiveRootView: View {
             // difensivo — non dovrebbe accadere (unico chiamante di navigate(.detail) è
             // onSelectShow sopra, che valorizza selectedSetlist nello stesso gesto).
             if let show = selectedSetlist {
-                QLiveShowDetailView(setlist: show, onBack: { navigate(to: .shows) })
+                QLiveShowDetailView(
+                    setlist: show,
+                    onBack: { navigate(to: .shows) },
+                    // ⟦S5b⟧ `Cond (a)` — ORDINE OBBLIGATO: PRIMA si riempie lo slot,
+                    // POI si naviga. Invertendo, il ramo `.metronome` si monta con
+                    // `runner == nil` e il ramo `else` SI VEDE per un frame.
+                    // L'irraggiungibilità di quel ramo è CONSEGUENZA di queste due
+                    // righe in quest'ordine, non un fatto indipendente.
+                    // ⛔ NESSUNO stop audio qui, e nessun avvio: `navigate` resta muto
+                    //    sul transport (decisione CD 18/07, :78-85). Questo è un
+                    //    INGRESSO — arma e basta. Il click parte al secondo tap
+                    //    (`LiveView.swift:134-137`).
+                    onStart: { runner in
+                        roomSession.install(runner)
+                        navigate(to: .metronome)
+                    }
+                )
             } else {
                 EmptyView()
             }
@@ -170,6 +186,20 @@ struct QLiveRootView: View {
                 // Oggi la pagina resta comunque IRRAGGIUNGIBILE: nessuno chiama
                 // `navigate(to: .metronome)`. L'unico chiamante dell'imbuto è
                 // il back del player qui sopra, e porta a `.shows`.
+                //
+                // ⚠️ MARCATURA ⟦S5b⟧ — LE DUE FRASI QUI SOPRA SONO SCADUTE. Si marcano,
+                //    NON si riscrivono: sono la storia di come ci si è arrivati.
+                //    (1) «⟦S5⟧ NON parte senza questo empty-state» — superata dalla
+                //        CANCELLAZIONE dell'atomo A3 empty-state
+                //        (`LIBRO_MASTRO_QBEATS.md:355`). Non serve alcun disegno CD: qui
+                //        il ramo `else` resta una GUARDIA DIFENSIVA, esattamente come il
+                //        gemello a :105-107 nello stesso `switch`. ⟦S5b⟧ è partita, e
+                //        senza empty-state.
+                //    (2) «nessuno chiama `navigate(to: .metronome)`» — falsa da ⟦S5b⟧:
+                //        lo chiama `onStart` qui sopra, e per `Cond (a)` lo chiama SEMPRE
+                //        dopo aver riempito lo slot. ⇒ il ramo resta irraggiungibile, ma
+                //        ora per una RAGIONE DIVERSA: non più «nessuno naviga qui»,
+                //        bensì «chi naviga qui ha già installato il runner».
                 EmptyView()
             }
         }
diff --git a/ios_app/QBeats/UI/QLive/QLiveSession.swift b/ios_app/QBeats/UI/QLive/QLiveSession.swift
index dfc4d29..59df815 100644
--- a/ios_app/QBeats/UI/QLive/QLiveSession.swift
+++ b/ios_app/QBeats/UI/QLive/QLiveSession.swift
@@ -31,6 +31,27 @@ import Combine
 final class QLiveSession: ObservableObject {
 
     /// Slot del runner. `nil` = nessuna scaletta in esecuzione.
-    /// `private(set)` e senza mutatore: in ⟦S4R⟧ non è riempibile da nessuno.
+    /// `private(set)`: da ⟦S5b⟧ si riempie SOLO da `install(_:)` qui sotto.
     @Published private(set) var runner: SetlistRunner? = nil
+
+    /// ⟦S5b⟧ — MUTATORE DELLO SLOT. È la porta che ⟦S4R⟧ aveva lasciato
+    /// mancante APPOSTA (:12-15), ed è il solo punto in cui il runner entra
+    /// nella stanza.
+    ///
+    /// ⛔ INSTALLA E BASTA: NON avvia l'audio, NON arma nulla, non tocca
+    /// AudioEngine. Contratto `BOX5_QBEATS.md:331` (QL-SHOWS-07) e
+    /// `BOX5_QBEATS.md:354` (§3): l'ingresso in uno show è SEMPRE arma +
+    /// standby, e il click parte al SECONDO tap. L'armamento d'ingresso vive
+    /// in `SetlistRunner.primeDisplay(session:)`, che gira nell'`onAppear` del
+    /// player — non qui.
+    ///
+    /// ⚠️ ORDINE OBBLIGATO (⟦S5b⟧ `Cond (a)`): si chiama PRIMA di navigare a
+    /// `.metronome`, mai dopo. A ordine invertito il gate `if let runner` di
+    /// `QLiveRootView` rende un frame col ramo `else`, e quel ramo SI VEDE.
+    ///
+    /// Un runner nuovo SOSTITUISCE il precedente: il vecchio perde il suo
+    /// ultimo riferimento forte e muore con lo scambio.
+    func install(_ newRunner: SetlistRunner) {
+        runner = newRunner
+    }
 }
diff --git a/ios_app/QBeats/UI/QLive/QLiveShowDetailView.swift b/ios_app/QBeats/UI/QLive/QLiveShowDetailView.swift
index 505fbcc..873e765 100644
--- a/ios_app/QBeats/UI/QLive/QLiveShowDetailView.swift
+++ b/ios_app/QBeats/UI/QLive/QLiveShowDetailView.swift
@@ -77,6 +77,20 @@ import SwiftUI
 struct QLiveShowDetailView: View {
     let setlist: Setlist
     let onBack: () -> Void
+    /// ⟦S5b⟧ — Start. Il runner si costruisce QUI, perché questa vista possiede
+    /// già i due soli ingredienti che servono: `setlist` (:78) e `store` (:81) —
+    /// `SetlistRunner.init(setlist:store:)` non chiede altro
+    /// (`SetlistRunner.swift:61`). Il presentatore lo installa nello slot di
+    /// stanza e POI naviga: ⟦S5b⟧ `Cond (a)`, ordine obbligato.
+    ///
+    /// ⚠️ DIVERGENZA DICHIARATA DALLA LETTERA DELLA SCHEDA, non dalla sua
+    /// sostanza. La scheda scrive «closure `() -> Void` … nella forma di
+    /// `onBack`», e nello stesso elenco prescrive che il runner si costruisca
+    /// IN QUESTO FILE citandone le righe :78 e :81. Le due cose stanno insieme
+    /// solo se la closure TRASPORTA il runner: una `() -> Void` obbligherebbe a
+    /// costruirlo nella root, contro la riga che lo assegna qui. Forma adottata:
+    /// `(SetlistRunner) -> Void`. Rilievo aperto nel referto A124.
+    let onStart: (SetlistRunner) -> Void
 
     @ObservedObject private var store = QBeatsStore.shared
 
@@ -287,9 +301,14 @@ struct QLiveShowDetailView: View {
     private func startfoot(_ resolved: (songs: [Song], missingIDs: [UUID])) -> some View {
         let isEnabled = !resolved.songs.isEmpty
         return Button {
-            // ⟦S5b⟧ cablerà qui l'avvio reale (SetlistRunner.startSetlist). Vuota apposta:
-            // eccezione dichiarata al divieto CD-Q7 sui bottoni morti (LIBRO v31), stessa
-            // forma dello slot senza mutatore di QLiveSession (⟦S4R⟧).
+            // ⟦S5b⟧ — ARMA, NON SUONA. Qui non si tocca l'audio: si costruisce il runner
+            // con la setlist SCELTA e lo si consegna al presentatore. ⛔ NON si chiama
+            // `startSetlist`: farebbe partire il click al PRIMO tocco, contro
+            // `BOX5_QBEATS.md:331` (QL-SHOWS-07, «l'ingresso in uno show è SEMPRE arma +
+            // standby, qualunque sia il flag standby della prima canzone») e
+            // `BOX5_QBEATS.md:354` (§3, «il click parte al secondo tap — schermo ovunque —
+            // o via MIDI»). Il secondo tap è già cablato: `LiveView.swift:134-137`.
+            onStart(SetlistRunner(setlist: setlist, store: store))
         } label: {
             HStack(spacing: 10) {
                 PlayGlyphShape()
@@ -333,6 +352,18 @@ struct QLiveShowDetailView: View {
             .opacity(isEnabled ? 1.0 : 0.4)   // token DS --disabled: 0.4
         }
         .buttonStyle(.plain)
+        // ⟦S5b⟧ `Cond (d)` — IL TOCCO SI CHIUDE su show vuoto o tutto-orfano. Fino a
+        // qui `isEnabled` pilotava SOLO l'aspetto — ombra (:323), bordo (:326-331),
+        // opacità (:333) — e il bottone SEMBRAVA spento restando tappabile: `.disabled(`
+        // rendeva 0 in questo file, mentre il corpus lo usa in 6 file. Senza questa riga
+        // un tocco su una scaletta orfana costruisce un runner a catalogo VUOTO,
+        // `primeDisplay` esce al primo `guard` (`SetlistRunner.swift:272`) e il player si
+        // monta BIANCO.
+        // ⚠️ DA GUARDARE AL GATE DEVICE, e non ho modo di vederlo io: `.disabled` agisce
+        // anche sull'ambiente. Se lo stile `.plain` applicasse un proprio smorzamento, lo
+        // Start spento risulterebbe più scuro dello 0,4 prescritto dal freeze (:333).
+        // Confronto: Frame F/G del freeze, `DESIGN/QLive_Nav/2026-07-11_Q7-Q16.html`.
+        .disabled(!isEnabled)
         .padding(.horizontal, 18)
         .padding(.top, 14)
         .padding(.bottom, 20)
