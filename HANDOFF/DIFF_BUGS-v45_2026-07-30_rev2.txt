diff --git a/BUGS_QBEATS.md b/BUGS_QBEATS.md
index f71ea7e..fc571fb 100644
--- a/BUGS_QBEATS.md
+++ b/BUGS_QBEATS.md
@@ -1,7 +1,7 @@
 # BUGS_QBEATS — Tracker centralizzato bug e tech debt
 
-**Versione:** 44
-**Ultima modifica:** 2026-07-28
+**Versione:** 45
+**Ultima modifica:** 2026-07-30
 **Autore iniziale:** CC chat principale 26/05/2026 sera
 **Repo:** `C:\Users\BULLFROG\Desktop\ANTIGRAVITY\Q-BEATS\`
 
@@ -347,8 +347,12 @@ Documento di riferimento **UNICO** per tutti i bug e tech debt (TD) Q-BEATS. Agg
 - **Riscontro a fonte (20/07, HEAD `6ded4ab`) — l'assenza è provata, non assunta:** il campo è un `TextField("", text: $searchText)` nudo (`UI/QLive/QLiveShowsView.swift:138`, dentro `searchRow` `:124`; placeholder disegnato a parte `:132-133`). Conteggio sul file: `FocusState` = 0 · `submitLabel` = 0 · `toolbar` = 0 · `scrollDismissesKeyboard` = 0 · `onSubmit` = 0 · `resignFirstResponder` = 0 · `searchable` = 0. **Non c'è nulla, in tutto il file, che possa chiudere la tastiera.**
 - **NON è una violazione del freeze — è un BUCO nel freeze:** nel contratto CD (`DESIGN/QLive_Nav/2026-07-11_Q7-Q16.html @ 9994bc0`) **non esiste alcuna specifica** di tastiera, dismissal o pulsante di cancellazione — zero occorrenze di `keyboard` / `tastiera` / `dismiss` / `clear` (verificato a fonte 20/07; le uniche corrispondenze di «cancel» sono sottostringhe di parole italiane — «Can**cell**o», «can**cell**ata» — non affordance UI). Il comportamento **non è mai stato specificato**, da nessuno. Domanda già inviata a CD (**Q20**).
 - **⚠️ TRIGGER DI RIVALUTAZIONE — è il cuore del ticket:** **all'apertura di ⟦S4L⟧ la severità sale a 🔴 ALTA.** Motivo: LIBRO v37 Sez.2 (riga 2026-07-18) ratifica che la proprietà del `SetlistRunner` sale alla stanza Q-Live e che il runner «**muore SOLO all'uscita dalla STANZA Q-Live**». Da S4L in poi, **l'unico modo oggi noto di chiudere la tastiera — uscire dalla stanza — UCCIDE LA SESSIONE LIVE.** Un difetto di comodità diventa un difetto che **spegne il metronomo a metà set**.
-- **Fix:** NON progettato. Attende la decisione CD (Q20).
-- **Stato:** 🟠 OPEN MEDIA / ⚠️ NON BLOCCANTE (**oggi**).
+- **⚠️ SUPERATO IN PARTE — 29/07: la parte principale è CHIUSA su device. I quattro bullet sopra restano ESATTAMENTE come sono scritti: sono storia, si marca e non si riscrive.** Q20 **ha avuto risposta** e il congedo è stato costruito: ⟦S4K⟧, commit `b9f4e5f0c806a40938136cd8bb076f590c5e851d`, CI `30456740570` **success**, gate device Mauro su iPad + iPhone il 29/07 — ratifica a monte in `LIBRO_MASTRO_QBEATS.md` Sez.2 riga `2026-07-30` ⟦S4K⟧. Costruito con **barra propria** via `.safeAreaInset(edge:.bottom)` (⛔ `ToolbarItemGroup(placement:.keyboard)` scartato PRIMA del commit: bug Apple documentato e mai risolto, forum thread 736040) più `.scrollDismissesKeyboard(.interactively)`; e la **ⓧ di svuotamento**, che il bullet «Fatto» dava per assente, ora esiste.
+- **I sette zeri del bullet «Riscontro a fonte» erano veri a `6ded4ab` e NON lo sono più.** Rimisurati a HEAD `4d1f9741f5947e7677c84d3f31a586e34b7709f3` sullo stesso file `ios_app/QBeats/UI/QLive/QLiveShowsView.swift` — NOTAZIONE: **righe-che-contengono**; controllo positivo stessa forma stesso file, `var body` → **1**. `FocusState` → **1** (`:51`) · `submitLabel` → **1** (`:191`) · `scrollDismissesKeyboard` → **1** (`:266`) · `onSubmit` → **1** (`:192`) · `xclearButton` → **2** (`:200` uso, `:219` definizione). Restano a **0**: `resignFirstResponder` · `searchable`. ⚠️ `toolbar` rende **1**, ma **NON è il modificatore SwiftUI**: è il commento `// .kbtoolbar (§CSS Q20)` a `:121`. L'assenza di `.toolbar` è **deliberata** (bug Apple sopra) — un conteggio per lemma, qui, avrebbe mentito in favore della tesi.
+- **⚠️ RESIDUO APERTO — QUARTA LACUNA, accanto alle tre che ⟦S4K⟧ dichiara non verificate.** ⟦S4K⟧ lascia scoperti, e li dichiara: tastiera fisica · tastiera flottante iPad · «il congedo conserva la selezione» (non testabile a HEAD perché `isPick` è cablato a false). **Se ne aggiunge una QUARTA, osservata su device da Mauro il 29/07: la tastiera SOPRAVVIVE al tasto HOME** — si esce dall'app con la tastiera alzata, e al rientro è ancora lì. ⛔ **Il MECCANISMO non è stabilito, e in questo ticket NON viene ipotizzato: entra il sintomo osservato, non la spiegazione.** Nessuna misura è stata fatta sul ciclo background/foreground per questo caso. L'unica cosa misurata è che il diff di ⟦S4K⟧ **non tocca il lifecycle**: sul diff di `b9f4e5f0`, NOTAZIONE righe-che-contengono, `background|HOME|ScenePhase` → **3**, e **nessuna delle tre è lifecycle** (una è il commento «`.kbtoolbar` background», due sono il modificatore di colore `.background(`); controllo positivo stessa forma sullo stesso diff, righe `^+` → **98**. Questo esclude che ⟦S4K⟧ abbia introdotto il residuo, **non** dice da dove venga.
+- **❓ DOMANDA APERTA, non indagata: succede anche in Q-Stage?** L'omologo campo di ricerca di Q-Stage (`ShowsListView`) non è mai stato provato al rientro da HOME. Finché non lo si sa, **non è dato dire se il residuo appartenga a Q-Live o all'app** — e la risposta cambia sia il dominio sia il fronte a cui il fix si aggancia. Verifica a carico di Mauro, una prova sola su device.
+- **Fix:** **la parte principale è FATTA** (⟦S4K⟧, bullet sopra). Per il **residuo HOME**: NON progettato, e **non progettabile** prima di sapere se il fenomeno è di Q-Live o dell'app — vedi la domanda aperta sopra.
+- **Stato:** 🟡 **RESIDUO APERTO.** La trappola originale — tastiera non chiudibile in nessun modo — è 🟢 **CHIUSA su device il 29/07**; resta aperta la sola **sopravvivenza a HOME**, ⚠️ **NON BLOCCANTE**. ⚠️ **Il TRIGGER di rivalutazione del bullet sopra non sale più a 🔴 con ⟦S4L⟧:** poggiava sul fatto che uscire dalla stanza fosse *l'unico* modo di chiudere la tastiera, e dal 29/07 non lo è più. ⛔ **E QUI SI APRE UNA PENDENZA, dichiarata invece che aggirata — è E3.** L'enunciato generale «da ⟦S4L⟧ in poi, uscire dalla stanza UCCIDE LA SESSIONE» **è ratificato a monte** in `LIBRO_MASTRO_QBEATS.md:285`, riga `2026-07-18` («il runner … **muore SOLO all'uscita dalla STANZA Q-Live**»), **ma in BUGS non ha alcun ticket che lo ospiti.** Misurato sul blob a HEAD `4d1f9741f5947e7677c84d3f31a586e34b7709f3`, NOTAZIONE righe-che-contengono: «uccide la sessione» → **1** · «muore SOLO» → **1** · «spegne il metronomo» → **1** · «uscita dalla stanza» → **1**, e **sono tutte e quattro la riga 349 di questo stesso ticket**, cioè il bullet qui sopra; controlli positivi stessa forma stesso blob, «SetlistRunner» → **8** · «Q-Live» → **38** · «runner» → **19**. ⚠️ **`TD-qlive-exit-unconfirmed-stop` (§1.1) NON è il ticket ospite:** tratta un'**altra** conseguenza dello stesso gesto — lo **stop propagato alla band** — non la morte del runner; e nasce da BUGS v39 (registro `| 39 |`) su ratifica LIBRO v36, che sono due righe distinte da quella del runner. ⇒ Uscendo questo ticket dal 🔴, l'enunciato **resta senza casa**. ⛔ **L'apertura del ticket che deve ospitarlo è DECISIONE DI MAURO e NON è stata presa qui.** ⚠️ **Il ticket RESTA in §1.2 benché la severità reale sia ora 🟡:** spostarlo eccede il mandato di questo giro e romperebbe i puntatori che lo indirizzano — si marca qui, si sposta quando qualcuno lo decide.
 - **Dominio:** CD (decisione) + CC (cablaggio).
 
 ## 📦 1.3 — Backlog (🟡 OPEN BASSA)
@@ -438,6 +442,7 @@ Documento di riferimento **UNICO** per tutti i bug e tech debt (TD) Q-BEATS. Agg
 - **Sintomo:** `get-task-allow=true` di TD #27 è compatibile solo con Development profile.
 - **Impatto:** pre-submission App Store servirà secondo file entitlements senza la chiave + variante CI.
 - **Insieme a:** TD #22 + TD #30 + TD #31.
+- **⇄ RIMANDO INCROCIATO (30/07), l'altra direzione:** questo ticket **non è mai stato né provato né chiuso** per una ragione precisa, ed è registrata in `TD-ci-nessuna-build-distribution` (§1.5 Prerequisiti di distribuzione) — **nessuna build Distribution è mai stata costruita** in questo repo (misurato: la CI dichiara `<key>method</key><string>development</string>` in `.github/workflows/ios_build.yml:92-93`, e `find -iname "ExportOptions*.plist"` sul repo → **0**; controllo positivo stessa forma stesso file, righe con `run:` → **10**). Non essendoci mai stata quella build, il secondo file entitlements non è mai servito e la sua assenza non si è mai manifestata. ⇒ **I due si chiudono insieme:** il primo tentativo di build Distribution è **anche** il primo collaudo di questo ticket.
 
 ### TD #33 — Formula bpb-dipendente in `scheduleBPMChange`
 - **Sintomo:** bypassata da `atNextBeat:true` ma residua per altri caller.
@@ -531,6 +536,9 @@ Documento di riferimento **UNICO** per tutti i bug e tech debt (TD) Q-BEATS. Agg
 - **Priorità:** 🟡 bassa / 📦 backlog. **Dominio:** CC (+ CD per gli ingressi UI).
 
 ### TD-emptystatekit-theme-dep — token condivisi di `EmptyStateKit` dipendono da `QStageKit` (🔵 COSMETICO / struttura — PRE-ESISTENTE, non introdotto da S2d)
+- **⚠️ PORTATA ALLARGATA 30/07 — il ticket non riguarda più UN file, ma 29 righe di codice su QUATTRO file.** Il titolo qui sopra e i bullet che seguono restano come sono scritti (storia, si marca e non si riscrive): quello che cambia è il **perimetro**. Misurato a HEAD `4d1f9741f5947e7677c84d3f31a586e34b7709f3` — `grep -rn "QStageTheme" ios_app/QBeats/UI/QLive/`, NOTAZIONE **righe-che-contengono**: **31 righe** su 4 file (`MetroFAB.swift` 5 · `QLiveEmptyStates.swift` 11 · `QLiveKit.swift` 2 · `QLiveShowsView.swift` 12; `QLiveRootView.swift` **0**). ⚠️ **Di quelle 31, DUE non sono codice**: `QLiveKit.swift:6` e `:13` sono commenti che *enunciano la regola di separazione*, non la violano — restano quindi **29 righe di codice reale** in cui la stanza Q-Live attinge al tema di Q-Stage. Controllo positivo stessa forma stesso perimetro: `QLiveTheme` → `QLiveKit.swift` 1 · `QLiveShowsView.swift` 6.
+- **Il difetto non è «un componente condiviso che pesca da una stanza»: è che la stanza Q-Live non ha un tema proprio abbastanza ricco.** `QLiveTheme` (`QLiveKit.swift:8-17`) espone **cinque** token e nessuno è di testo — `surf`, `bd`, `pick`, `accent`, `kbbar` (controllo positivo stessa forma: `Color(hex:` in quel file → **4**). Per ogni colore di testo Q-Live **deve** attingere a `QStageTheme`, perché alternativa non ne esiste. Il `CoreTheme` neutro proposto nel bullet «Fix possibile» qui sopra resta la direzione giusta, ma il fix dimensionato su un solo file era sottodimensionato.
+- **Famiglia di tre ticket che si CITANO a vicenda e NON si fondono:** questo (igiene: da dove attinge il codice) · `TD-qlive-token-text2r-non-onorato` (contratto: il codice non onora un token che CD ha ratificato) · `TD-design-system-non-ancorato` (documentale: i documenti del DS non concordano fra loro, voce ⑨ già cita `QStageTheme.text3`). **Sono tre difetti distinti sulla stessa area** e chiuderne uno non chiude gli altri due — motivo per cui restano separati.
 - **Fatto tecnico (a HEAD `ab6b553`):** `EmptyStateLayout` (`UI/Components/EmptyStateKit.swift`, `.foregroundColor(QStageTheme.text3)`) usa un token di `QStageKit` — il tema della stanza Q-Stage. Un componente in `UI/Components/`, per natura condiviso Q-Stage/Q-Live (Q13 «No shows yet» di Q-Stage lo riusa), dipende così dal tema di UNA stanza. Compila (stesso modulo Swift), ma è un odore: un "condiviso" legato a una stanza specifica.
 - **Attribuzione:** PRE-ESISTENTE — il colore era già `QStageTheme.text3` in `QLiveEmptyStates.swift` PRIMA dell'estrazione (verificato a fonte: `git show 7550476:…QLiveEmptyStates.swift:67`, dentro `EmptyStateLayout`). S2d (move puro, `ab6b553`) ha solo RILOCATO la dipendenza, NON l'ha introdotta. `QLiveEmptyStates` continua a usare lo stesso token nella subview Ⓔ.
 - **Fix possibile (solo annotato, NON in questo commit):** un `CoreTheme` neutro che ospiti i token realmente condivisi (`text3` ecc.), da cui attingano sia `QStageKit` sia i componenti di `EmptyStateKit` — così `UI/Components/` non dipende più da un tema di stanza. Valutazione a sé, fuori perimetro S2d.
@@ -571,6 +579,40 @@ Documento di riferimento **UNICO** per tutti i bug e tech debt (TD) Q-BEATS. Agg
 - **Stato:** 🟡 OPEN BASSA / 📦 BACKLOG. Nessun impatto utente, nessun rischio palco. **Calendario: l'atomo va DOPO il traguardo §6** — il «dopo §6» del giro 27-28/07 era un vincolo di CALENDARIO (dopo il traguardo §6 del piano atomi), non una collocazione nel file; la posizione in coda a §1.3 è corretta e resta.
 - **Dominio:** CC (igiene codice) + CD (coerenza dei documenti DS).
 
+### TD-qlive-token-text2r-non-onorato — il codice non onora `--text2r`, token introdotto da Q20 rev2 (🟡 OPEN BASSA / **violazione di contratto CD**, non igiene)
+- **Cos'è, in una riga:** il contratto CD Q20 rev2 introduce un token di testo nuovo, `--text2r`, e lo assegna a quattro elementi di Q-Live › Shows; nel codice a HEAD **quel token non esiste** e i quattro elementi portano due token diversi, uno dei quali a **metà dell'opacità** chiesta.
+- **Il contratto (fonte, con impronta):** `2026-07-26_QLive-Shows-Keyboard-Dismiss__Q20-RIEMISSIONE.html`, sha256 `5DCFBBFBFEFE7607CFDAC58B73FB2DDB2898012788D75F9827203E1C5AB9D408`, 37430 byte, 308 righe — **su E:, NON nel repo** (vedi la riga `2026-07-30` ARTEFATTI NORMATIVI FUORI DAL CONTROLLO DI VERSIONE in `LIBRO_MASTRO_QBEATS.md` Sez.2). Definizione a `:31` → `--text2r:rgba(255,255,255,0.60);`. Applicato a **quattro** elementi: `.search` `:82` · `.search .mg` `:83` · `.search .ph` `:84` · `.showrow .mt` `:96`. NOTAZIONE righe-che-contengono, sul file su disco: `text2r` → **9**; controllo positivo stessa forma stesso file, `qlive-surf` → **3**.
+- **Il codice a HEAD `4d1f9741f5947e7677c84d3f31a586e34b7709f3` — il token non esiste proprio:** `grep -rn "text2r" ios_app/` → **0** righe. Controlli positivi stessa forma stesso albero: `text2` → **29** · `text3` → **31**. Non è «scritto male»: **non è mai stato portato nel codice**.
+- **Cosa portano invece i tre elementi implementati** (`ios_app/QBeats/UI/QLive/QLiveShowsView.swift`): lente `:172` → `QStageTheme.text3` · placeholder `:181` → `QStageTheme.text3` · meta `.mt` `:286` → `QStageTheme.text2`. Valori a `ios_app/QBeats/UI/QStage/QStageKit.swift:13-14`: `text2 = white 0.55`, `text3 = white 0.30`. ⚠️ **Contro lo `0.60` chiesto, la lente e il placeholder rendono `0.30` — la metà.** Non è uno scarto di sfumatura: è il testo di servizio della ricerca a metà della leggibilità prevista, su un'app che si usa al buio su un palco.
+- **Non è una svista: è un contratto PRECEDENTE rimasto in piedi.** `QLiveShowsView.swift:175` porta il commento «Placeholder colorato **--text3**» — il codice dichiara sé stesso allineato a `--text3`, cioè al contratto anteriore a Q20 rev2. Quando rev2 ha introdotto `--text2r`, il codice non è stato riallineato e **nessuno se n'è accorto** perché il commento sembrava coerente.
+- **Perché è un ticket a sé e non igiene:** `TD-emptystatekit-theme-dep` chiede *da dove* il codice attinge (architettura); qui il codice attinge da dove può, ma **rende un valore che CD non ha ratificato** (contratto). Chiudere l'uno non chiude l'altro. Si citano, non si fondono — vedi anche `TD-design-system-non-ancorato`.
+- **Fix:** promuovere `--text2r` a token di `QLiveTheme` (`QLiveKit.swift`, che oggi non ha alcun token di testo) e cablarlo ai quattro elementi; rettificare il commento `:175`, che oggi indirizza al contratto sbagliato. ⚠️ Da fare **insieme** al fix di `TD-emptystatekit-theme-dep`, o si tocca due volte lo stesso file.
+- **Stato:** 🟡 OPEN BASSA / 📦 BACKLOG. Nessun rischio palco, impatto di leggibilità reale ma non bloccante. **Dominio:** CC (cablaggio) — CD ha già ratificato, non c'è nulla da chiedere.
+
+### TD-qlive-search-active-non-costruito — lo stato «search attiva» è specificato da Q20 e non esiste nel codice (🟡 OPEN BASSA / contratto CD non costruito)
+- **Cos'è:** Q20 rev2 specifica un secondo stato visivo del campo di ricerca — `.search.active`, quando il campo ha il fuoco — e il codice a HEAD rende il campo **sempre identico**, con o senza fuoco.
+- **Il contratto:** `2026-07-26_QLive-Shows-Keyboard-Dismiss__Q20-RIEMISSIONE.html:86` → `.search.active{border-color:rgba(212,63,0,0.75); background:#1a1614; box-shadow:0 0 0 3px rgba(212,63,0,0.16);}`, e il markup che lo esercita è a `:219` (`<div class="search active">`, con la lente virata a `#ff8a5c`). Non è una regola orfana: il contratto la disegna.
+- **Il codice a HEAD:** `ios_app/QBeats/UI/QLive/QLiveShowsView.swift:203-214` — sfondo `QLiveTheme.surf` e bordo `QLiveTheme.bd`, **entrambi costanti**, nessuna condizione su `searchFieldFocused`. Il fuoco esiste ed è già osservato dalla vista (`@FocusState` a `:51`, NOTAZIONE righe-che-contengono: `searchFieldFocused` → **6** righe; controllo positivo stessa forma stesso file, `searchText` → **7**): **manca solo di essere letto dal ramo di stile.**
+- **⚠️ E il colore chiesto ESISTE GIÀ come token, inutilizzato per questo scopo:** `#1a1614` è esattamente `QLiveTheme.pick` (`ios_app/QBeats/UI/QLive/QLiveKit.swift:11`). Oggi `QLiveTheme.pick` ha **1 sola** occorrenza in tutto `ios_app/` (`QLiveShowsView.swift:298`, il ramo `isPick` che è cablato a `false`); controllo positivo stessa forma stesso albero, `QLiveTheme.surf` → **2**. ⇒ Il token è in casa, è di fatto morto, e il suo secondo impiego legittimo è proprio questo.
+- **Fix:** legare sfondo e bordo del contenitore `.search` a `searchFieldFocused`, riusando `QLiveTheme.pick` per lo sfondo attivo. Piccolo, ma **non farlo dentro ⟦S4R⟧**: è un atomo di stile, non di navigazione.
+- **Stato:** 🟡 OPEN BASSA / 📦 BACKLOG. **Dominio:** CC. CD ha già ratificato con Q20 rev2.
+
+### TD-linkkit-commenti-versione-stale — tre righe di commento dichiarano una versione di LinkKit che non è quella in uso (🟡 OPEN BASSA / documentale nel codice)
+- **Il fatto, misurato a HEAD `4d1f9741f5947e7677c84d3f31a586e34b7709f3`** — NOTAZIONE **righe-che-contengono**: **3 righe stale-di-versione su 9 menzioni LinkKit** — **2 righe «LinkKit 3.2.2»** in `ios_app/QBeats/AudioEngine.swift:447,457` e **1 riga «LinkKit 3.x»** in `ios_app/QBeats/LinkEngine.mm:55`. **Due file, testi diversi**: non è una singola frase ripetuta, sono due formulazioni indipendenti che invecchiano ciascuna per conto proprio. Conteggi: `LinkKit` → 2 righe in `AudioEngine.swift`, 7 in `LinkEngine.mm` (totale 9); controllo positivo stessa forma stesso file, `ABLLink` in `LinkEngine.mm` → **105**.
+- **La versione vera è 4.0, e il codice lo dice pure — nello stesso file che dice 3.x:** `LinkEngine.mm:2` → «API C pura di LinkKit **4.0**». ⇒ Il file si contraddice da solo a 53 righe di distanza. Il vendor è a 4.0 dal commit `42424ef` (05/05/2026) — **[R]**, non rimisurato alla sorgente in questo giro.
+- **Perché non è cosmetico quanto sembra:** questi commenti sono l'unica documentazione di *quale* comportamento di LinkKit si sta assumendo, su una libreria **closed-source** dove il comportamento non si può leggere altrove. Il ticket `TD-link-indicator-stale` (§1.2) poggia esplicitamente su un'ipotesi circa il callback interno di LinkKit: chi la rivaluta leggendo «3.2.2» rivaluta contro la libreria sbagliata. **Un commento di versione stale, su una dipendenza opaca, è una trappola per il prossimo che indaga.**
+- **Fix:** allineare le tre righe a 4.0, oppure — meglio — togliere il numero e citare la fonte di versione, così non c'è più nulla da tenere aggiornato a mano. ⚠️ Prima di scrivere «4.0», riverificare la versione **alla sorgente dell'SDK**: nel giro del 29/07 non è stato trovato un marcatore di versione indipendente dentro il vendor.
+- **Stato:** 🟡 OPEN BASSA / 📦 BACKLOG. **Dominio:** CC.
+
+### TD-censimento-e-cartella-annidata — il censimento dell'archivio E: non vede le cartelle annidate, e una misura incisa ci si è già rotta contro (🟡 OPEN BASSA / metodo di misura — famiglia «artefatti fuori dal controllo di versione»)
+- **Il fatto:** `LIBRO_MASTRO_QBEATS.md` Sez.2, riga `2026-07-30` INDIRIZZO, NON COPIA, incide tre cifre di censimento come prova che «il difetto non è del sistema di cartelle»: **BOX3 94 su 94 · BOX5 35 · SCALETTA 5**, tutti versionati. Rimisurate il 30/07 sotto `E:\HOBBY\MUSICA - BATTERIA -SISTA\Q-BEATS\FILE X CLAUDE.MD\` — NOTAZIONE: **file che il pattern trova**, `find -type f -iname`: BOX5 → **35** (coincide) · SCALETTA → **5** (coincide) · **BOX3 → 97, contro le 94 incise**. Due cifre su tre reggono: **non è il metodo a essere rotto in generale, è un caso che gli sfugge.**
+- **⚠️ NON È UN FILE SINGOLO, È UNA CARTELLA.** La scomposizione dei 97 per cartella: `BOX3_Codice` **91** · `DUPLICATI` **1** · `HANDOFF` **4** · **`HANDOFF/REFEREE_SYNC_2026-07-13` 1**. È quest'ultima il reperto: una cartella **annidata di due livelli** dentro `HANDOFF/`, che un censimento condotto per cartella di primo livello non guarda.
+- **Cosa contiene, misurato:** tre file — `BOX3_V94_2026-07-13.md` (39198 byte), `BOX5_V25_2026-07-13.md` (30122 byte), **`LIBRO_MASTRO_QBEATS33.md`** (98497 byte). Il primo è **byte-identico** al suo omologo in `BOX3_Codice/` (sha256 `A1CDCB1F06EB3CC90124742FC5B218048B1DE61A4049D988D15E40F6F2882FBE` su entrambi): un doppione innocuo per il contenuto, **non** per il conteggio.
+- **⚠️ E il terzo file porta la convenzione di nome VECCHIA** — `LIBRO_MASTRO_QBEATS33.md`, senza `_V`, senza data e senza commit: la stessa forma di `BUGS_QBEATS43.md`. ⇒ La cartella che sfugge al censimento è anche **il posto dove sopravvive la nomenclatura che il censimento voleva dichiarare estinta**. Le due cose non sono indipendenti: **non è mai stato censito, quindi non è mai stato normalizzato.**
+- **La lezione, che è il vero contenuto del ticket:** una misura di censimento incisa in un canonico ha **dichiarato completezza** («94 su 94, nessuna eccezione») su un perimetro che non copriva tutto l'albero. L'impronta era vera, il conteggio era vero *per ciò che guardava*, e la conclusione era comunque più forte del dato. ⛔ **Un censimento va dichiarato insieme al PERIMETRO che ha spazzato**, o «nessuna eccezione» significa solo «nessuna eccezione dove ho guardato».
+- **Fix:** NESSUNA cancellazione, NESSUNO spostamento — `REFEREE_SYNC_2026-07-13/` è un **reperto di consegna datato nel path** e va deciso a parte, esattamente come la terza copia LIBRO in `DA_CD_PER_CC\11_07_2026\…\uploads\`. Quello che va fatto è (a) rieseguire il censimento **ricorsivo** e dichiararlo tale, (b) rettificare la cifra incisa quando si toccherà quella riga per altro. ⛔ **La riga `2026-07-30` del LIBRO NON si corregge qui:** è storia, e per la regola ratificata in LIBRO v45 la sua chiave di data è un indirizzo. Si marca, e questo ticket È la marcatura.
+- **Stato:** 🟡 OPEN BASSA / 📦 BACKLOG. Nessun impatto su codice, prodotto o palco. **Dominio:** CC (metodo) + Mauro (sorte dei reperti di consegna).
+
 ## 1.4 — Backlog UX puro (📦, dominio CD)
 
 Riferimento `LIBRO_MASTRO_QBEATS.md` Sezione 3 deliverable per il dettaglio:
@@ -586,6 +628,60 @@ Riferimento `LIBRO_MASTRO_QBEATS.md` Sezione 3 deliverable per il dettaglio:
 
 Questi NON sono bug ma deliverable UX. Listati qui per completezza visiva del backlog ma il primario è `LIBRO_MASTRO_QBEATS.md` Sezione 3.
 
+## 🚢 1.5 — PREREQUISITI DI DISTRIBUZIONE (bloccano la SPEDIZIONE, non il palco)
+
+Blocco aperto il **30/07/2026**. Nasce da un fatto nuovo messo agli atti il giorno prima e non prima: **l'app si vende** (`LIBRO_MASTRO_QBEATS.md` Sez.2, riga `2026-07-30` «L'APP SI VENDE», dichiarazione Mauro 29/07). Finché il traguardo era «suonarci sopra», nulla di quanto segue era un difetto; da quando il traguardo è «spedirla», ognuna di queste voci è una **porta chiusa fra la CI verde di oggi e una build che possa esistere sullo Store**.
+
+> **Semantica di questo blocco, distinta dalle altre:** §1.1 misura il rischio sul palco, §1.2 il rischio sulla release funzionale, §1.3 il debito. Qui la domanda è una sola e diversa: **«questa build può essere spedita?»** Un ticket può stare qui ed essere 🟢 innocuo dal vivo. ⚠️ Per la regola posizionale delle Convenzioni (punto 5) la posizione non porta significato, lo porta la severità: questo blocco **non è un grado di severità**, è un **asse ortogonale**. Un ticket qui dentro dichiara la propria severità per conto proprio.
+
+> **Due prerequisiti vivono già altrove e NON sono duplicati qui — si citano:**
+> · **`TD #32` — Dual entitlements file Dev/Distribution** (§1.3): `get-task-allow=true` è compatibile col solo profilo Development, e la pre-submission richiederà un secondo file entitlements più la variante CI. È il **gemello a monte** di `TD-ci-nessuna-build-distribution` qui sotto: quello dice che il file mancherà, questo che non lo si è mai potuto provare.
+> · **`SettingsView` — unico ingresso dietro `#if DEBUG`** (§1.2): alla **prima build Release** l'unico ingresso a Settings compila via, e l'utente resta senza `linkMode` e senza pannello Ableton. Oggi non morde solo perché la CI archivia in `-configuration Debug`. È un prerequisito di distribuzione a tutti gli effetti, ma resta in §1.2 dove è nato e dove i puntatori lo indirizzano.
+
+### TD-buildnumber-mai-fissato — il numero di build del prodotto è `1`, e `project.yml` non lo fissa affatto (🟠 OPEN MEDIA / 🚢 prerequisito di distribuzione — innocuo in sideload)
+- **Il fatto misurato, in tre punti che vanno letti insieme.** (a) Nel **prodotto reale** `CFBundleVersion` vale **`'1'`** — letto dall'`Info.plist` **binario** dentro `Payload/QBeats.app` dell'IPA della run `30463659772`, con `plistlib`; controllo positivo stessa lettura, **36 chiavi totali** nel plist e `CFBundleIdentifier` = `'com.bullfrog.qbeats'`, `UIUserInterfaceStyle` = `'Dark'`, `MinimumOSVersion` = `'16.0'`. (b) Nel **file fisico** `ios_app/QBeats/Info.plist:10-11` c'è `CFBundleVersion` → `142`; controllo positivo stessa forma stesso file, righe con `key` → **14**. (c) In **`ios_app/project.yml`**, che è ciò che comanda davvero, le chiavi di numero-build sono **ZERO** — NOTAZIONE righe-che-contengono, pattern `CFBundleVersion|CURRENT_PROJECT_VERSION|MARKETING_VERSION`; controllo positivo stessa forma stesso file, `QBeats` → **17**.
+- **La diagnosi, ed è il motivo per cui i due ticket separati erano uno solo.** Non è «il file fisico dice una cosa e il prodotto un'altra» — quello è il *sintomo*. È che **`project.yml` non fissa alcun numero di build**, quindi XcodeGen non riceve nulla da scrivere e **il prodotto eredita il default**, che è `1`. Il `142` del file fisico non viene sovrascritto con un altro valore: **non viene proprio considerato**, perché XcodeGen quel file lo **rigenera** (`Docs/ProjectSpec.md`, verbatim: «This is the path where the plist will be written to»). ⇒ **Un fix solo**, non due: dichiarare il numero di build in `project.yml`.
+- **Quando morde, e quanto — ⚠️ [R], e la formulazione conta più del fatto.** In **sideload è innocuo**: si reinstalla e basta. Verso **TestFlight e App Store** la regola **NON è di unicità, è di CRESCITA MONOTONA**, e la differenza non è accademica: un build `5` caricato dopo un build `8` verrebbe rifiutato **pur essendo unico**. Con `CFBundleVersion` inchiodato a `1` il numero non cresce mai, quindi il problema **si ripresenta a ogni consegna**, non una volta sola. ⚠️ **La documentazione ufficiale Apple NON è stata raggiunta e questo claim resta [R]:** `developer.apple.com` risponde «questa pagina richiede JavaScript» e la fonte normativa non è verificabile da qui. **Corroborazione, e va presa per quello che è:** il testo letterale dell'errore di validazione di App Store Connect, riprodotto su un thread dei forum Apple (`developer.apple.com/forums/thread/693838`) — «*The bundle version, NNN, must be a higher than the previously uploaded version*» (il refuso «a higher» è nell'originale, ed è ciò che rende riconoscibile la stringa). ⛔ **È CONTENUTO GENERATO DAGLI UTENTI, NON documentazione ufficiale:** vale come indizio sulla **formulazione** della regola — «*higher than the previously uploaded*», cioè crescita — e **non** come prova della regola. ⚠️ Nota di onestà: il bullet **Fix** qui sotto diceva già la cosa giusta («un valore che **cresca** a ogni build»); a essere formulato male era questo bullet, che parlava di unicità.
+- **⚠️ Precedente da non dimenticare, ed è il motivo per cui questo ticket esiste come misura e non come impressione:** la stessa famiglia di errore ha già prodotto un quasi-sbaglio — dedurre lo stato del prodotto dal file fisico invece che dal prodotto. Fra `Info.plist` e l'IPA c'è **XcodeGen**, e chi legge il file fisico legge un input che nessuno usa.
+- **Fix:** una sola voce in `ios_app/project.yml`, sotto `info.properties` oppure come `CURRENT_PROJECT_VERSION` nelle build settings, con un valore che **cresca a ogni build** (contatore CI, non costante a mano). ⛔ **NON** toccare `ios_app/QBeats/Info.plist`: è un file rigenerato, e scriverci dentro è la trappola che questo ticket documenta.
+- **Verifica di chiusura:** non «il `project.yml` contiene la chiave», ma **`CFBundleVersion` letto dall'`Info.plist` binario di un IPA prodotto dalla CI**. La prova sta nel prodotto, non nella sorgente.
+- **Stato:** 🟠 OPEN MEDIA / ⚠️ NON BLOCCANTE per il palco / 🚢 **BLOCCANTE per la distribuzione**. **Dominio:** CC.
+
+### TD-ci-xcodegen-non-pinnato — la CI installa XcodeGen senza fissarne la versione (🟡 OPEN BASSA / riproducibilità della build)
+- **Il fatto:** entrambi i workflow installano lo strumento con `brew install xcodegen`, **senza alcun vincolo di versione**, e poi ci generano il progetto: `.github/workflows/ios_build.yml:14` e `:18`, `.github/workflows/f1_build_check.yml:14` e `:18` (NOTAZIONE righe-che-contengono, pattern `xcodegen`: **2** per file; controllo positivo stessa forma stessi file, `runs-on` → **1** per file). ⇒ Due build dallo stesso commit, a distanza di tempo, possono usare due XcodeGen diversi e produrre due progetti diversi — **senza che nulla lo segnali**.
+- **Perché sta fra i prerequisiti di distribuzione e non nel debito generico:** XcodeGen **genera l'`Info.plist` e gli entitlements** del prodotto. Una build di spedizione deve essere riproducibile, e qui il generatore del progetto è una variabile libera. È lo stesso strumento che sta a monte di `TD-buildnumber-mai-fissato` e che è già stato **causa root** di un guasto reale.
+- **⚠️ Il precedente esiste ed è nello stesso file:** `TD #44` (Sez.2, chiuso 23/05/2026) ha come causa root proprio `xcodegen generate` che sovrascriveva `QBeats.entitlements` con un `<dict/>` vuoto → binario firmato senza `com.apple.developer.networking.multicast` → discovery Link QB↔QB rotta. **Il comportamento di XcodeGen ha già rotto il prodotto una volta, in modo invisibile alla CI.**
+- **Nota di misura, dichiarata perché una formulazione più comoda sarebbe stata falsa:** in BUGS v44 ci sono **zero ticket sul pinning**; l'**unica occorrenza del lemma** `xcodegen` è a `r.698`, dentro `TD #44`, ed è **altro argomento** (la causa root storica, non la versione dello strumento). Controllo positivo stessa forma stesso file: `TD #44` → **10** righe. Dire «zero occorrenze» sarebbe stato comodo e sbagliato.
+- **Fix:** fissare la versione all'installazione in entrambi i workflow, e allinearla a quella con cui il progetto è stato validato. **Insieme a:** `TD #22` (pin Xcode runner GitHub Actions), che è lo stesso problema sull'altro strumento.
+- **Stato:** 🟡 OPEN BASSA / 🚢 prerequisito di distribuzione. **Dominio:** CC.
+
+### TD-zero-diagnostica-di-guasto — un crash sul palco di un cliente non lascia traccia raggiungibile (🟠 OPEN MEDIA / **cecità post-vendita**)
+- **Il fatto:** in tutto `ios_app/` non esiste **alcuna** raccolta di guasti — NOTAZIONE righe-che-contengono, pattern `Crashlytics|Sentry|Bugsnag|MetricKit|NSSetUncaughtExceptionHandler` → **0 righe**, exit 1. Controllo positivo stessa forma stesso albero: file che usano `os_log` o `Logger(` → **17**.
+- **Il punto non è che manchi la strumentazione: è dove finisce quella che c'è.** I 17 file loggano, e loggano bene — ma su `os_log`, che si legge **solo con il device fisico in mano e iMazing collegato**. È il metodo con cui questo progetto ha risolto TD#17, e funziona finché il device è quello di Mauro. **Su un device venduto a un cliente, di un crash a metà concerto non arriva assolutamente nulla**: nessun report, nessuno stack, nessun conteggio. Si saprebbe solo che «si è chiusa», e da lì non si riparte.
+- **Perché 🟠 e non 🟡:** non c'è un fix di codice che questo ticket richieda con urgenza, ma la sua assenza **si paga dopo la vendita e non prima** — cioè quando costa di più e non si può più rimediare retroattivamente sulle build già in mano alla gente. Fra le voci di questo blocco è la sola il cui costo cresce col tempo invece che restare fermo.
+- **⚠️ Da decidere PRIMA di scegliere lo strumento, ed è una decisione di Mauro, non tecnica:** un raccoglitore di crash di terze parti invia dati fuori dal device. Va deciso **che cosa esce**, e va detto all'utente. `MetricKit` è l'opzione che resta in casa Apple e non introduce un terzo.
+- **Fix:** NESSUNO proposto qui. Prima la decisione sul dato, poi lo strumento. **Dominio:** Mauro (decisione) + CC (integrazione).
+- **Stato:** 🟠 OPEN MEDIA / ⚠️ NON BLOCCANTE oggi / 🚢 prerequisito di distribuzione.
+
+### TD-ci-nessuna-build-distribution — la CI ha prodotto solo IPA firmati Development, e la categoria Distribution non è mai stata esercitata (🟠 OPEN MEDIA / **l'intera catena di spedizione è non provata**)
+- **Il fatto, quattro punti indipendenti nello stesso file** `.github/workflows/ios_build.yml` (NOTAZIONE righe-che-contengono, pattern `Distribution|Development|get-task-allow|provisioning|CODE_SIGN|export-method|exportMethod` → **4 righe**; controllo positivo stessa forma stesso file, `run:` → **10**): `:51` `CODE_SIGNING_ALLOWED=NO` (fase di test) · `:81` `CODE_SIGN_IDENTITY="Apple Development"` · `:98` `provisioningProfiles` → `:101` `QBeats_Dev_Profile` · `:108` `codesign --force --sign "Apple Development: …"`. E l'`ExportOptions.plist` che la CI genera in linea dichiara **`<key>method</key><string>development</string>`** (`:92-93`), `signingStyle` `manual` (`:96-97`), `teamID` `X42CX3ZP3T` (`:94-95`). ⇒ **Nessun ramo Distribution esiste, nemmeno spento.** Non c'è un `ExportOptions.plist` alternativo nel repo: `find -iname "ExportOptions*.plist"` → **0**.
+- **«CI verde» non vuol dire «pronto per lo Store», e la distanza è una categoria intera, non un flag.** Mancano: certificato e profilo di Distribution, `get-task-allow:false`, un `ExportOptions` con `method: app-store`, e la **riverifica dell'entitlement multicast sul profilo di Distribuzione**.
+- **⚠️ Il dubbio sul multicast è CONCRETO e ha un precedente in questo stesso file.** `TD #44` (Sez.2, 23/05/2026): il permesso multicast **c'era sul portale** e si perse **comunque** nella firma, perché la generazione del progetto svuotava il file entitlements — e il sintomo fu la discovery Link QB↔QB rotta, cioè la funzione centrale dell'app. Un entitlement che sopravvive alla firma Development **non è prova** che sopravviva a quella Distribution: è una firma diversa, con un profilo diverso.
+- **⇄ RIMANDO INCROCIATO con `TD #32` (§1.3), esplicito nelle due direzioni.** `TD #32` dice che alla pre-submission servirà un **secondo file entitlements** senza `get-task-allow`, più la variante CI. **Questo ticket è la ragione per cui TD #32 non è mai stato né provato né chiuso:** non essendoci mai stata una build Distribution, il secondo file non è mai stato necessario e la sua assenza non si è mai manifestata. I due si chiudono insieme o non si chiudono: il primo tentativo di build Distribution è **anche** il primo collaudo di TD #32.
+- **Fix:** costruire **una** build Distribution, anche solo per vedere che cosa si rompe. Non è un fix, è la misura che oggi manca. ⚠️ Mauro l'ha segnalata come «da fare presto, non dopo il §6».
+- **Stato:** 🟠 OPEN MEDIA / ⚠️ NON BLOCCANTE per il palco / 🚢 **BLOCCANTE per la distribuzione**. **Dominio:** CC (workflow) + Mauro (certificati e profilo sul portale).
+
+### TD-icona-mai-integrata — l'app non ha icona: non è «da disegnare», è un'INTEGRAZIONE mai fatta (🟠 OPEN MEDIA / 🚢 prerequisito di distribuzione — bundle incompleto, misurato)
+- **Nel prodotto reale non c'è nulla di grafico.** Misurato il 30/07 sull'IPA della run `30463659772`, estratto e ispezionato: file il cui nome contiene `icon` → **0** · file `.png` → **0** · cataloghi compilati `.car` → **0**. Controlli positivi stessa forma stesso bundle: file **totali** → **35**, di cui `.ttf` → **22**. **Il bundle non è vuoto e non è troncato**: ci sono i 22 font (6 Inter + 16 JetBrains Mono), `click.wav`, `test_backtrack.mp3`, il binario `QBeats`, `embedded.mobileprovision`, `_CodeSignature`, `ZIPFoundation_ZIPFoundation.bundle`, `PkgInfo`, `Info.plist`. **Font, audio, binario e firma: presenti. Grafica: nessuna.**
+- **E nell'`Info.plist` binario del prodotto non c'è nemmeno la dichiarazione:** `CFBundleIcons` · `CFBundleIcons~ipad` · `CFBundleIconFiles` · `CFBundleIconName` → **4 su 4 assenti**. Ricerca larga sulla stessa lettura, chiavi che contengano `icon` in qualunque forma → **nessuna**. Controllo positivo: **36 chiavi totali** nel plist.
+- **Nel repo non esiste neppure il contenitore.** `grep -rniE "appicon|icon_name|CFBundleIcon"` su `ios_app/` e `.github/` → **0 righe**; controllo positivo stessa forma, `QBeats` in `ios_app/project.yml` → **17**. Cataloghi grafici: `.xcassets` → **0** · `.imageset` → **0** · `.colorset` → **0**; controlli positivi stesso albero, `.swift` → **66**, `.yml` → **4**. ⇒ **Non è «zero varianti dentro il catalogo»: il catalogo non esiste.**
+- **⚠️ E QUI IL TICKET CAMBIA NATURA — [R], dichiarato da Mauro il 30/07 e non misurato da CC:** l'artefatto grafico dell'icona **esiste già, ma è fuori dal progetto**. ⇒ Il lavoro **non è «disegnare l'icona»**. È **un'integrazione mai fatta**, in quattro passi: reperire il file · **verificarne la conformità ai requisiti Apple** (dimensioni, formato, canale alpha) — ⚠️ **[R] NON SORGENTATO: i requisiti puntuali non sono stati verificati**, `developer.apple.com` non è raggiungibile da qui e nessuna fonte è agli atti; **vanno letti prima di preparare l'asset, non dopo** · **creare il catalogo grafico che oggi non esiste** · dichiararlo in `ios_app/project.yml`.
+- **⛔ La dichiarazione va in `project.yml`, MAI nel file fisico `Info.plist`.** Stessa trappola di `TD-buildnumber-mai-fissato` qui sopra: quel file è **rigenerato** da XcodeGen e ciò che vi si scrive non arriva al prodotto. È la seconda voce di questo blocco che poggia sullo stesso equivoco — e per questo le due condividono il fix, non solo la causa.
+- **Perché è un prerequisito duro — e la ragione NON è un divieto di Apple, è una misura.** Il bundle che oggi si spedirebbe è **incompleto in modo misurato**: zero file grafici e **quattro chiavi icona su quattro assenti**, letti dal prodotto reale qui sopra. Quella è la ragione del ticket, e regge da sola. ⚠️ **[R] NON SORGENTATO, e per questo NON è la motivazione del ticket:** che una submission priva di icona venga **rifiutata** è convinzione diffusa ma **non verificata qui** (documentazione Apple non raggiungibile) — non va citata come fatto, né usata per giustificare la severità. Dal vivo invece l'assenza non ha alcun effetto: l'icona non si guarda mentre si suona. È il caso più netto di voce che vale 🟢 sul palco e 🚢 alla spedizione.
+- **⚠️ Nota di custodia, stessa famiglia del contratto Q20:** l'artefatto esiste **fuori dal controllo di versione**, come il contratto Q20 normativo di ⟦S4K⟧ — vedi `LIBRO_MASTRO_QBEATS.md` Sez.2, riga `2026-07-30` ARTEFATTI NORMATIVI FUORI DAL CONTROLLO DI VERSIONE, dove i due sono già classificati insieme. **Il primo passo del fix è quindi anche una messa in sicurezza**, non solo un'integrazione.
+- **Classificazione:** debito tecnico, **non materia CD**. Test di scope applicato — «CD deve saperlo o coordinarsi?» → **no**: l'artefatto esiste già e non c'è nulla da disegnare.
+- **Stato:** 🟠 OPEN MEDIA / 🟢 nessun effetto sul palco / 🚢 **BLOCCANTE per la distribuzione**. **Dominio:** CC (integrazione) + Mauro (consegna del file).
+
 ---
 
 # Sezione 2 — Bug CHIUSI (storico, non si cancellano)
@@ -905,6 +1001,7 @@ Per data di chiusura, decrescente.
 | 42 | 2026-07-22 | Mauro + CC + referee | **TD#17 — condizione di chiusura AGLI ATTI (doc-only), ordine R-γ rispettato: prima LIBRO, poi BUGS.** Nuovo bullet «CONDIZIONE DI CHIUSURA» in §1.1/TD #17 + clausola «NON 🟢 CHIUSO» dello Stato allineata: TD#17 si chiude con **(A)** run di palco reale 2-3 h su VR2800 in **banda singola** senza perdita peer **OPPURE (B)** Soluzione C in produzione — **alternative, non cumulative**; il cross-banda sotto roaming = **caratterizzazione**, in coda, NON condizione. Supersede nel ticket la formula del 22/06 («restano caso roam reale sul VR2800 + run palco 2-3h», AND); il resto del quadro 22/06 invariato (root cause H388X, VR2800 pulito, single-band). Decisione Mauro 21/07; ratifica a monte in **LIBRO v40** Sez.2 riga 2026-07-22 (commit `131a511`) — fino ad allora la condizione viveva solo in handoff/memorie e per R-γ non esisteva. Stato TD#17 invariato: 🟠 OPEN MEDIA, NON bloccante. Bump header 41→42. Doc-only, nessun fix codice. |
 | 43 | 2026-07-23 | Mauro + CC + referee | **⟦FIX-PILL⟧ chiuso su device + 1 ticket nuovo.** `TD-qstage-pill-qlive-morta` → 🟢 **CHIUSO 23/07**, spostato in Sez.2 (Luglio 2026, in testa per data decrescente): commit `ee31281`, CI `29994513488` verde, gate device Mauro iPhone+iPad, 4 tocchi hit-area 4/4 PASS — ora osservabili a occhio invece che dalla Console. Fix = 3 file L3 (`QStageRootView` espone `onSwitchToLive`, `AppRootView` lo inietta esplicito, `ShowsListView` lo chiama al posto della sonda `[GATE-S3]`), +19/−5, sweep call-site pulito prima del commit (nessuna toolchain locale: prima prova = CI). **§1.3 (🟡):** `TD-qstage-tab-reset` — rientrando in Q-Stage la linguetta riparte da Songs; **pre-esistente**, provato dalla controprova Home → Q-Stage (atterra su Songs senza Q-Live coinvolta), NON regressione di `ee31281`; il montaggio condizionale è invariante Nodo A e non si tocca; domanda CD aperta (Shows o Songs al rientro). Header bump 42→43. |
 | 44 | 2026-07-28 | Mauro + CC + referee | **Propagazioni giro colore + 2 ticket nuovi + 1 chiusura by-design (doc-only), ordine R-γ rispettato: prima LIBRO v42, poi BUGS.** `TD-qstage-tab-reset` → 🟢 **CHIUSO 26/07 by-design**, spostato in Sez.2: ratifica LIBRO Sez.2 riga `2026-07-26` "TAB-RESET Q-STAGE" (Mauro «si»); la propagazione era FERMA — BUGS a v43 aveva ZERO occorrenze di «2026-07-26» (controllo positivo «2026-07-23» → 2): stallo di propagazione, non disaccordo. Allineata IN COPPIA la riga «strascico» del ticket ⟦FIX-PILL⟧ in Sez.2, che chiudendo il ticket diventava stale nello stesso istante (`HANDOFF_CC_2026-07-27_chiusura-giro-colore.txt` sez. 4: «vanno toccate in coppia»). **§1.3 +2:** `TD-setlist-accumulo` (`deleteSetlist` zero call-site @ `8fa5018…`, due precondizioni di architettura dichiarate) · `TD-design-system-non-ancorato` (🟡 bassa, dieci voci ①-⑩ dal giro token 27/07; collocato in CODA a §1.3 — il puntatore «dopo §6» del prompt non risolve in BUGS, sezioni 1-5 e nessun §6, misurato; ⚠️ testo verbatim da `HANDOFF/HANDOFF_CC_2026-07-27_chiusura-giro-colore.txt` sez. 3.3 sha256 `d9e2a39b…b499f41f` — la fonte dichiarata, `MISURE_G3_…`, NON contiene il testo: zero occorrenze del titolo, controlli positivi 13/3). **§1.2:** `TD-qlive-libero-limbo` +4 bullet "ANGOLO DEL RIORDINO" (`moveSetlists` a `Store/QBeatsStore.swift:120 @ 8fa5018…`: un riordino cambia quale show Q-Live caricherebbe; riferimento `LiveRootView.swift:8` riancorato a path+simbolo per STALENESS — era corretto a `59ab33e`, stale dal 17/07 con Nodo A — non perché falso). Header bump 43→44. Doc-only, nessun fix codice. |
+| 45 | 2026-07-30 | Mauro + CC + referee | **Blocco NUOVO §1.5 «Prerequisiti di distribuzione» (5 ticket) + 4 ticket nuovi in §1.3 + 2 ticket esistenti allargati + 1 rimando incrociato (doc-only).** Nasce dal fatto messo agli atti in LIBRO v44 riga `2026-07-30` «L'APP SI VENDE»: da traguardo-palco a traguardo-spedizione, e una categoria di difetti che prima non esisteva. **§1.5 (asse ORTOGONALE alla severità, non un grado):** `TD-buildnumber-mai-fissato` (fusione dei due ticket separati «CFBundleVersion=1» e «Info.plist generato»: la diagnosi è **una sola** — `project.yml` non fissa ALCUNA chiave di numero-build, quindi XcodeGen non riceve nulla e il prodotto eredita il default `1`; prodotto `'1'` letto da `plistlib` sull'`Info.plist` binario dell'IPA run `30463659772`, file fisico `142` a `Info.plist:10-11`, `project.yml` **0** su `CFBundleVersion|CURRENT_PROJECT_VERSION|MARKETING_VERSION` con controllo positivo `QBeats`→17) · `TD-ci-xcodegen-non-pinnato` (`brew install xcodegen` senza versione in **entrambi** i workflow, `:14`/`:18`; ⚠️ formulazione tarata a fonte: **zero ticket sul pinning**, unica occorrenza del lemma a `r.698` dentro TD #44 = **altro argomento**, controllo positivo «TD #44»→10) · `TD-zero-diagnostica-di-guasto` (0 righe su `Crashlytics|Sentry|Bugsnag|MetricKit|NSSetUncaughtExceptionHandler`, controllo positivo 17 file con `os_log`/`Logger(`; la strumentazione c'è ma si legge **solo con device + iMazing** — di un guasto sul palco di un cliente non arriva nulla) · `TD-ci-nessuna-build-distribution` (`method: development` a `ios_build.yml:92-93`, 4 punti Development indipendenti, `ExportOptions*.plist` nel repo → 0; precedente **TD #44**, dove il multicast c'era sul portale e si perse **comunque** nella firma) · `TD-icona-mai-integrata` (prodotto reale: 0 file icona · 0 `.png` · 0 `.car`, controlli positivi 35 file totali e 22 `.ttf`; 4 chiavi `CFBundleIcon*` su 4 assenti, controllo positivo 36 chiavi; repo: 0 `.xcassets`/`.imageset`/`.colorset`, controllo positivo 66 `.swift`. ⚠️ **[R] Mauro 30/07: l'artefatto esiste ma è fuori dal progetto** ⇒ il ticket NON è «disegnare», è **integrare**). **§1.3 +4:** `TD-qlive-token-text2r-non-onorato` (violazione di **contratto CD**, tenuto separato dall'igiene: Q20 rev2 `:31` definisce `--text2r` = white **0.60** su 4 elementi, nel codice `text2r`→**0** occorrenze e lente/placeholder rendono `QStageTheme.text3` = white **0.30**, **la metà**; il commento `QLiveShowsView.swift:175` dichiara ancora `--text3`, cioè il contratto ANTERIORE) · `TD-qlive-search-active-non-costruito` (Q20 `:86` specifica `.search.active`, il codice rende il campo sempre identico; ⚠️ il colore chiesto `#1a1614` **è già** `QLiveTheme.pick`, oggi con **1** sola occorrenza su un ramo cablato a false) · `TD-linkkit-commenti-versione-stale` (**3 righe stale-di-versione su 9 menzioni**: 2 righe «LinkKit 3.2.2» in `AudioEngine.swift:447,457` + 1 riga «LinkKit 3.x» in `LinkEngine.mm:55` — **due file, testi diversi**; lo stesso file dice «LinkKit 4.0» a `:2`, si contraddice a 53 righe di distanza) · `TD-censimento-e-cartella-annidata` (la cifra incisa «BOX3 **94 su 94**» rimisurata rende **97**, mentre BOX5 **35** e SCALETTA **5** coincidono: non è il metodo a essere rotto, è una **CARTELLA ANNIDATA** che gli sfugge — `HANDOFF/REFEREE_SYNC_2026-07-13/`, 3 file, di cui un BOX3 byte-identico e un `LIBRO_MASTRO_QBEATS33.md` in convenzione di nome **vecchia**; ⛔ la riga di LIBRO **non si corregge**, si marca). **Allargati, senza riscrivere nulla:** `TD-qlive-search-keyboard-trap` — Q20 **ha avuto risposta** e la trappola è 🟢 **CHIUSA device 29/07** (⟦S4K⟧ `b9f4e5f0`, CI `30456740570`); i sette zeri del bullet «Riscontro a fonte», veri a `6ded4ab`, sono rimisurati a HEAD e cinque non lo sono più (⚠️ `toolbar`→1 è un **commento**, non il modificatore: l'assenza è deliberata per il bug Apple 736040). Resta un **RESIDUO**: la tastiera **sopravvive al tasto HOME** [device Mauro 29/07] — **quarta lacuna** accanto alle tre dichiarate da ⟦S4K⟧, con il **meccanismo NON stabilito e deliberatamente non ipotizzato**, e la domanda «succede anche in Q-Stage?» lasciata aperta; misurato solo che il diff di ⟦S4K⟧ non tocca il lifecycle (3 righe su `background|HOME|ScenePhase`, nessuna di lifecycle, controllo positivo 98 righe `^+`). Ticket lasciato in §1.2 benché la severità reale scenda a 🟡: spostarlo eccede il mandato e romperebbe i puntatori. · `TD-emptystatekit-theme-dep` — portata da **1 file a 29 righe di codice su 4 file** (`QStageTheme` in `UI/QLive/` → **31** righe, **meno 2** che sono commenti che *enunciano* la regola invece di violarla; controllo positivo `QLiveTheme` → 1+6). I **tre della famiglia si citano e NON si fondono**. **⇄ Rimando incrociato bidirezionale** fra `TD #32` e `TD-ci-nessuna-build-distribution`: si chiudono insieme, il primo tentativo di build Distribution è anche il primo collaudo di TD #32. ⚠️ **PENDENZA APERTA, non risolta qui:** uscendo `TD-qlive-search-keyboard-trap` dal 🔴, l'enunciato «da ⟦S4L⟧ in poi uscire dalla stanza uccide la sessione» — ratificato in `LIBRO:285` — **resta senza un ticket che lo ospiti in BUGS** (misurato: le 4 formulazioni rendono 1 riga ciascuna, ed è sempre la r.349 di quello stesso ticket; controlli positivi «SetlistRunner»→8, «Q-Live»→38). È E3, e **l'apertura del ticket ospite è decisione di Mauro**. ⚠️ **Tre claim su Apple riformulati prima del commit** (rilievo referee): la regola del build number è **crescita monotona, non unicità** — marcata **[R]**, corroborata dal testo letterale dell'errore App Store Connect su un thread dei forum Apple (**contenuto utente, NON documentazione ufficiale**); i claim «Apple rifiuta le icone con trasparenza» e «senza icona non supera la submission» ridotti a **[R] non sorgentati** e rimossi dalle motivazioni dei ticket — `developer.apple.com` non è raggiungibile da qui. La severità 🚢 dei due ticket regge sulle sole misure. Header bump 44→45. Doc-only, nessun fix codice. |
 
 ---
 
