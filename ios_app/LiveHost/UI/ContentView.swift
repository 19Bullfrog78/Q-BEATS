// UI iPhone-first, portrait. HDMI external display gestito separatamente in HDMIOutputManager.swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("LiveHost Ready")
    }
}
