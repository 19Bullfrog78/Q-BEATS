import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Q-Beats")
                .font(.largeTitle)
                .bold()
            Text("Motore C++ Inizializzato")
                .foregroundColor(.green)
        }
    }
}
